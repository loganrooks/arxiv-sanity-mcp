---
phase: 04.1-mcp-v1-expose-existing-services-as-mcp-tools-and-resources
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arxiv_mcp/interest/ranking.py
  - src/arxiv_mcp/db/models.py
  - src/arxiv_mcp/workflow/triage.py
  - src/arxiv_mcp/interest/search_augment.py
  - alembic/versions/007_add_seen_triage_state.py
  - tests/test_interest/test_ranking.py
  - tests/test_workflow/test_triage.py
  - tests/test_interest/test_search_augment.py
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PREMCP-01
  - PREMCP-02
  - PREMCP-03

must_haves:
  truths:
    - "Category overlap is scored exactly once in the ranking pipeline (effective weight = 0.15)"
    - "'Seen' triage state can be set on a paper and is distinct from absence (unseen) and from decisions (shortlisted/dismissed)"
    - "Over-fetch pagination limitation is documented in _ranked_search docstring and verified by test"
    - "ROADMAP.md and REQUIREMENTS.md reflect the actual MCP tool/resource design from CONTEXT.md"
  artifacts:
    - path: "src/arxiv_mcp/interest/ranking.py"
      provides: "Fixed score_seed_relation and score_profile_match without redundant category Jaccard"
      contains: "author_score"
    - path: "src/arxiv_mcp/db/models.py"
      provides: "TriageState CHECK constraint including 'seen'"
      contains: "'seen'"
    - path: "src/arxiv_mcp/workflow/triage.py"
      provides: "TriageService.VALID_STATES including 'seen'"
      contains: "'seen'"
    - path: "alembic/versions/007_add_seen_triage_state.py"
      provides: "Migration to update TriageState CHECK constraint"
      contains: "seen"
    - path: "src/arxiv_mcp/interest/search_augment.py"
      provides: "Documented _ranked_search with pagination limitation"
      contains: "approximate"
    - path: "tests/test_interest/test_search_augment.py"
      provides: "Docstring assertion test for PREMCP-03 pagination documentation"
      contains: "approximate"
  key_links:
    - from: "src/arxiv_mcp/interest/ranking.py"
      to: "tests/test_interest/test_ranking.py"
      via: "Test assertions verify effective category weight"
      pattern: "category_weight.*0\\.15"
    - from: "src/arxiv_mcp/db/models.py"
      to: "alembic/versions/007_add_seen_triage_state.py"
      via: "CHECK constraint matches migration"
      pattern: "'seen'"
    - from: "src/arxiv_mcp/interest/search_augment.py"
      to: "tests/test_interest/test_search_augment.py"
      via: "Docstring content verified by assertion"
      pattern: "approximate"
---

<objective>
Fix three pre-MCP quality issues before exposing services via MCP: ranking triple-counting bug, missing "seen" triage state, and undocumented pagination limitation. Also align ROADMAP.md and REQUIREMENTS.md with the actual MCP design from CONTEXT.md.

Purpose: The first real MCP usage must not be undermined by known defects. These are surgical fixes to existing code, not new features. Doc alignment ensures roadmap/requirements match the actual MCP design decided during discussion.
Output: Fixed ranking pipeline, "seen" triage state, documented pagination approximation, aligned planning docs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04.1-mcp-v1-expose-existing-services-as-mcp-tools-and-resources/04.1-CONTEXT.md
@src/arxiv_mcp/interest/ranking.py
@src/arxiv_mcp/db/models.py
@src/arxiv_mcp/workflow/triage.py
@src/arxiv_mcp/interest/search_augment.py
@tests/test_interest/test_ranking.py
@tests/test_workflow/test_triage.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix ranking triple-counting and document pagination</name>
  <files>src/arxiv_mcp/interest/ranking.py, src/arxiv_mcp/interest/search_augment.py, tests/test_interest/test_ranking.py, tests/test_interest/test_search_augment.py</files>
  <behavior>
    - Test: score_seed_relation for a paper with matching categories and no authors returns 0.0 (category overlap removed, only author signal remains)
    - Test: score_seed_relation for a paper with matching author returns > 0.0 (author signal preserved)
    - Test: score_profile_match for a paper with matching categories and no authors returns only query_boost portion (category sub-signal removed)
    - Test: score_profile_match for a paper with matching author returns > 0.0 (author signal dominant)
    - Test: RankingPipeline.score_paper with DEFAULT_WEIGHTS produces category overlap weight exactly equal to DEFAULT_WEIGHTS[CATEGORY_OVERLAP] (0.15), not 0.375
    - Test: Verify no category Jaccard computation exists in score_seed_relation or score_profile_match
    - Test: _ranked_search docstring contains "approximate" (PREMCP-03 verification)
  </behavior>
  <action>
    **PREMCP-01: Fix ranking triple-counting (CRITICAL)**

    In `src/arxiv_mcp/interest/ranking.py`:

    1. In `score_seed_relation` (around line 275-291): Remove the "Sub-signal 1: category overlap with seeds" block entirely (lines computing `cat_score` via Jaccard). Change the composite from `0.6 * cat_score + 0.4 * author_score` to just `author_score`. The seed relation signal now focuses purely on author match. Update the explanation string to reflect author-only scoring.

    2. In `score_profile_match` (around line 338-358): Remove the "Sub-signal 1: category overlap" block (lines computing `cat_score` via Jaccard). Change the composite from `0.5 * cat_score + 0.35 * author_score + 0.15 * query_boost` to `0.85 * author_score + 0.15 * query_boost`. Redistribute freed category weight to author match as the primary signal. Update explanation string.

    3. Update existing tests in `tests/test_interest/test_ranking.py` that assert specific composite scores for seed_relation and profile_match -- their expected values will change because category Jaccard is removed. Add new test `test_category_weight_no_triple_count` that verifies: when a paper matches only via categories (no author match, no query match), the total weighted category contribution comes exclusively from `score_category_overlap` with weight 0.15.

    **PREMCP-03: Document over-fetch pagination limitation**

    In `src/arxiv_mcp/interest/search_augment.py`, update the `_ranked_search` method docstring (around line 165-172) to document: "Note: Pagination is approximate when profile-based re-ranking is active. Each page independently over-fetches N*3 results from the base service, re-ranks, and trims to N. This means page boundaries shift between requests -- a paper on page 1 could move to page 2 or vice versa on subsequent queries. This is a known limitation of the over-fetch + re-rank strategy."

    **PREMCP-03: Docstring assertion test**

    Create `tests/test_interest/test_search_augment.py` with a test `test_ranked_search_pagination_documented` that imports `_ranked_search` (or the class containing it) from `src/arxiv_mcp/interest/search_augment.py` and asserts the docstring contains "approximate". This ensures the pagination limitation documentation is not accidentally removed. Example:

    ```python
    from arxiv_mcp.interest.search_augment import ProfileRankingService

    def test_ranked_search_pagination_documented():
        """PREMCP-03: Over-fetch pagination limitation must be documented."""
        doc = ProfileRankingService._ranked_search.__doc__
        assert doc is not None, "_ranked_search must have a docstring"
        assert "approximate" in doc.lower(), (
            "_ranked_search docstring must document approximate pagination"
        )
    ```

    Adapt the import path and method name to match the actual class/method structure in search_augment.py.
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && uv run pytest tests/test_interest/test_ranking.py tests/test_interest/test_search_augment.py -x --timeout=30 -v</automated>
  </verify>
  <done>score_seed_relation computes only author match (no category Jaccard). score_profile_match computes 0.85*author + 0.15*query_boost (no category Jaccard). Effective category weight in the pipeline is exactly 0.15. _ranked_search docstring documents approximate pagination. Docstring assertion test passes. All ranking tests pass with updated expectations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add "seen" triage state with migration</name>
  <files>src/arxiv_mcp/db/models.py, src/arxiv_mcp/workflow/triage.py, alembic/versions/007_add_seen_triage_state.py, tests/test_workflow/test_triage.py</files>
  <behavior>
    - Test: mark_triage(arxiv_id, "seen") creates a TriageState row (not absence)
    - Test: mark_triage(arxiv_id, "seen") followed by get_triage_state returns "seen"
    - Test: "seen" is distinct from "unseen" (absence of row)
    - Test: "seen" is in TriageService.VALID_STATES
    - Test: Transitioning from "seen" to "shortlisted" works correctly and logs the transition
    - Test: Transitioning from "unseen" (no row) to "seen" creates a row with state="seen"
  </behavior>
  <action>
    **PREMCP-02: Restore "seen" triage state**

    1. In `src/arxiv_mcp/db/models.py` (line 260-263): Update the CheckConstraint string to include 'seen': `"state IN ('seen', 'shortlisted', 'dismissed', 'read', 'cite-later', 'archived')"`.

    2. In `src/arxiv_mcp/workflow/triage.py` (line 35): Add `"seen"` to `VALID_STATES`: `VALID_STATES = {"unseen", "seen", "shortlisted", "dismissed", "read", "cite-later", "archived"}`. The existing `mark_triage` method already handles non-"unseen" states by creating/updating a row, so "seen" works automatically -- it creates a TriageState row with state="seen". No new service method needed.

    3. Create `alembic/versions/007_add_seen_triage_state.py`: Drop old CHECK constraint `ck_triage_state_valid`, create new one including 'seen'. Set `revision = "007"`, `down_revision = "006"`. The downgrade drops the new constraint and recreates the old one without 'seen'.

    4. In `tests/test_workflow/test_triage.py`: Add tests for "seen" state: marking a paper as "seen" creates a row, get_triage_state returns "seen", transitioning from "seen" to "shortlisted" works, transitioning from no-row to "seen" creates a row. Verify "seen" is in VALID_STATES.
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && uv run pytest tests/test_workflow/test_triage.py -x --timeout=30 -v -k "seen"</automated>
  </verify>
  <done>"seen" is in TriageService.VALID_STATES and DB CHECK constraint. mark_triage("seen") creates a row (not absence). Transition from unseen->seen and seen->shortlisted both work. Migration 007 exists with correct up/down. All triage tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Align ROADMAP.md and REQUIREMENTS.md with CONTEXT.md MCP design</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    The CONTEXT.md from /gsd:discuss-phase represents more recent user decisions that supersede the original roadmap wording. Update planning docs to match the actual MCP design.

    **ROADMAP.md SC#4 update:**
    Change: "An MCP client can manage workflow state via create_collection, add_to_collection, mark_triage_state, create_saved_query, and get_delta_since_checkpoint tools"
    To: "An MCP client can manage workflow state via triage_paper, add_to_collection, and create_watch tools, plus add_signal for interest profiles and enrich_paper for enrichment"

    Rationale: CONTEXT.md defines 9 tools total. The workflow tools are triage_paper (not mark_triage_state), add_to_collection, and create_watch (which subsumes create_saved_query + promote_to_watch). get_delta_since_checkpoint is exposed as a resource (watch://{slug}/deltas), not a tool. add_signal and enrich_paper are separate tool groups.

    **ROADMAP.md SC#5 update:**
    Change: "MCP resources expose paper, collection, saved query, and result set as canonical objects"
    To: "MCP resources expose paper, collection, profile, and watch deltas as canonical objects"

    Rationale: CONTEXT.md specifies 4 resources: paper://{arxiv_id}, collection://{slug}, profile://{slug}, watch://{slug}/deltas. "Saved query" and "result set" are not separate resources -- saved queries are accessed through watches, and result sets are returned by tools.

    **REQUIREMENTS.md MCP-02 update:**
    Change: "MCP server exposes workflow tools: create_collection, add_to_collection, mark_triage_state, create_saved_query, get_delta_since_checkpoint"
    To: "MCP server exposes workflow/interest/enrichment tools: triage_paper, add_to_collection, create_watch, add_signal, enrich_paper"

    **REQUIREMENTS.md MCP-04 update:**
    Change: "MCP server exposes canonical resources: paper, collection, saved query, result set"
    To: "MCP server exposes canonical resources: paper://{arxiv_id}, collection://{slug}, profile://{slug}, watch://{slug}/deltas"
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && grep -c "triage_paper" .planning/ROADMAP.md && grep -c "profile.*slug" .planning/ROADMAP.md && grep -c "triage_paper" .planning/REQUIREMENTS.md && grep -c "profile.*slug\|watch.*deltas" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>ROADMAP.md SC#4 lists the actual 5 non-discovery tools (triage_paper, add_to_collection, create_watch, add_signal, enrich_paper). ROADMAP.md SC#5 lists the actual 4 resources (paper, collection, profile, watch deltas). REQUIREMENTS.md MCP-02 and MCP-04 match the CONTEXT.md design. No stale tool/resource names remain in planning docs.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_interest/test_ranking.py tests/test_interest/test_search_augment.py tests/test_workflow/test_triage.py -x --timeout=30` -- all test files pass
2. `uv run pytest tests/ -x --timeout=30` -- full suite passes (no regressions)
3. Grep confirms no category Jaccard in score_seed_relation or score_profile_match: `grep -n "cat_score\|Jaccard\|intersection.*union" src/arxiv_mcp/interest/ranking.py` should show only score_category_overlap function
4. Grep confirms "seen" in models: `grep "seen" src/arxiv_mcp/db/models.py src/arxiv_mcp/workflow/triage.py`
5. Grep confirms pagination doc: `grep -i "approximate" src/arxiv_mcp/interest/search_augment.py`
6. Grep confirms doc alignment: `grep "triage_paper" .planning/ROADMAP.md .planning/REQUIREMENTS.md`
</verification>

<success_criteria>
- Category overlap effective weight is exactly 0.15 (verified by test)
- "seen" triage state is functional end-to-end
- Migration 007 exists and is well-formed
- _ranked_search docstring documents approximate pagination (verified by docstring assertion test)
- ROADMAP.md and REQUIREMENTS.md reflect CONTEXT.md MCP design
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-mcp-v1-expose-existing-services-as-mcp-tools-and-resources/04.1-01-SUMMARY.md`
</output>
