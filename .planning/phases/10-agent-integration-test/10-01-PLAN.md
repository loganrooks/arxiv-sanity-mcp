---
phase: 10-agent-integration-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .mcp.json
autonomous: false
requirements: [SC-1, SC-4]

must_haves:
  truths:
    - "MCP server 'arxiv-discovery' appears in `claude mcp list` output with Connected status"
    - "Database has Phase 5 data (126 papers, 1 profile, 98 triage states) and migrations at head (008)"
    - "Agent can see all 13 tools from arxiv-discovery when asked to list MCP tools in a fresh Claude Code session"
    - "search_papers returns results from the real database through MCP (not empty/error)"
  artifacts:
    - path: ".mcp.json"
      provides: "Project-scoped MCP server configuration for Claude Code"
      contains: "arxiv-discovery"
  key_links:
    - from: ".mcp.json"
      to: "src/arxiv_mcp/mcp/server.py"
      via: "stdio transport launching venv Python with -m arxiv_mcp.mcp"
      pattern: "arxiv_mcp\\.mcp"
    - from: "MCP server"
      to: "PostgreSQL arxiv_mcp database"
      via: "DATABASE_URL in env block"
      pattern: "postgresql.*arxiv_mcp"
---

<objective>
Configure the arxiv-discovery MCP server in Claude Code and verify end-to-end connectivity from agent to database.

Purpose: Establish the integration point -- the MCP server must be configured, connected, and returning real data before any research session can begin. This also validates that the README's MCP configuration instructions are correct (or identifies corrections needed).

Output: Working MCP server connection confirmed by tool listing and live search query. `.mcp.json` in project root for reproducible configuration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-agent-integration-test/10-CONTEXT.md
@.planning/phases/10-agent-integration-test/10-RESEARCH.md
@README.md
@src/arxiv_mcp/mcp/server.py
@src/arxiv_mcp/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure MCP server and verify database state</name>
  <files>.mcp.json</files>
  <action>
1. Verify database state first:
   - Run `cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && .venv/bin/alembic current` to confirm migration head is 008
   - Run a quick Python script to verify data exists: papers count (expect ~126), interest_profiles (expect 1), triage_states (expect ~98), collections (expect 1), interest_signals (expect ~13)
   - If migrations are behind, run `.venv/bin/alembic upgrade head`

2. Create `.mcp.json` in the project root with this exact configuration:
```json
{
  "mcpServers": {
    "arxiv-discovery": {
      "type": "stdio",
      "command": "/home/rookslog/workspace/projects/arxiv-sanity-mcp/.venv/bin/python",
      "args": ["-m", "arxiv_mcp.mcp"],
      "cwd": "/home/rookslog/workspace/projects/arxiv-sanity-mcp",
      "env": {
        "DATABASE_URL": "postgresql+asyncpg://arxiv_mcp:arxiv_mcp_dev@localhost:5432/arxiv_mcp"
      }
    }
  }
}
```

   Use absolute venv Python path (not `python`), include `cwd` for pydantic-settings .env discovery, and pass DATABASE_URL explicitly in the env block (belt-and-suspenders per research pitfall 2).

3. Verify server starts without errors by running:
   `/home/rookslog/workspace/projects/arxiv-sanity-mcp/.venv/bin/python -c "from arxiv_mcp.mcp.server import mcp; print('Server module loaded successfully')"`

4. Add `.mcp.json` to `.gitignore` if not already present (it contains machine-specific paths).
  </action>
  <verify>
    <automated>.venv/bin/python -c "from arxiv_mcp.mcp.server import mcp; print('OK')" && cat .mcp.json | python3 -m json.tool > /dev/null && echo "Config valid"</automated>
  </verify>
  <done>Database confirmed at migration 008 with Phase 5 data. .mcp.json created with correct config. Server module imports cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify MCP server connects in Claude Code</name>
  <files>.mcp.json</files>
  <action>
Human verification that the MCP server connects end-to-end in Claude Code. The automated Task 1 created the .mcp.json config; this checkpoint verifies Claude Code picks it up and can use it to return real data.

Steps:
1. Start a NEW Claude Code session in the arxiv-sanity-mcp project directory (the current session will not pick up .mcp.json changes)
2. When Claude Code starts, it should prompt to approve the project-scoped MCP server from .mcp.json -- approve it
3. Type `/mcp` to check server status -- arxiv-discovery should appear
4. Ask: "List the available MCP tools from arxiv-discovery" -- should show 13 tools
5. Ask: "Search for papers about consciousness" -- should return results from the database (not empty)
6. If step 5 returns results, the server is fully connected end-to-end

If any step fails:
- Step 2 (no prompt): Check `claude mcp list` from terminal -- if not there, run: `claude mcp add-json arxiv-discovery '{"type":"stdio","command":"/home/rookslog/workspace/projects/arxiv-sanity-mcp/.venv/bin/python","args":["-m","arxiv_mcp.mcp"],"cwd":"/home/rookslog/workspace/projects/arxiv-sanity-mcp","env":{"DATABASE_URL":"postgresql+asyncpg://arxiv_mcp:arxiv_mcp_dev@localhost:5432/arxiv_mcp"}}' --scope local`
- Step 4 (no tools): Check for import errors in server startup
- Step 5 (no results): Check DATABASE_URL is pointing to arxiv_mcp (not arxiv_mcp_test)
  </action>
  <verify>
    <automated>echo "Manual checkpoint: user confirms MCP server connects in Claude Code session"</automated>
  </verify>
  <done>MCP server appears as "Connected" in Claude Code. All 13 tools visible. Live search returns papers from production database.</done>
</task>

</tasks>

<verification>
- .mcp.json exists with valid JSON and arxiv-discovery server config
- Database at migration 008 with Phase 5 data intact
- MCP server connects in Claude Code and returns real search results
</verification>

<success_criteria>
- MCP server appears as "Connected" in Claude Code
- All 13 tools visible to the agent
- Live search query returns papers from the production database
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-integration-test/10-01-SUMMARY.md`
</output>
