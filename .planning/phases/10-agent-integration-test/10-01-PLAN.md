---
phase: 10-agent-integration-test
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [SC-1, SC-4]

must_haves:
  truths:
    - "MCP server 'arxiv-discovery' appears in `claude mcp list` output with Connected status"
    - "Database has Phase 5 data (126 papers, 1 profile, 98 triage states) and migrations at head (008)"
    - "Agent can see all 13 tools from arxiv-discovery when asked to list MCP tools in a fresh Claude Code session"
    - "search_papers returns results from the real database through MCP (not empty/error)"
  artifacts:
    - path: "~/.claude.json"
      provides: "MCP server configuration for Claude Code (local scope, alongside existing servers)"
      contains: "arxiv-discovery"
  key_links:
    - from: "~/.claude.json mcpServers.arxiv-discovery"
      to: "src/arxiv_mcp/mcp/server.py"
      via: "stdio transport launching venv Python with -m arxiv_mcp.mcp"
      pattern: "arxiv_mcp\\.mcp"
    - from: "MCP server"
      to: "PostgreSQL arxiv_mcp database"
      via: "DATABASE_URL in env block"
      pattern: "postgresql.*arxiv_mcp"
---

<objective>
Configure the arxiv-discovery MCP server in Claude Code and verify end-to-end connectivity from agent to database.

Purpose: Establish the integration point -- the MCP server must be configured, connected, and returning real data before any research session can begin. This also validates that the README's MCP configuration instructions are correct (or identifies corrections needed).

Output: Working MCP server connection confirmed by tool listing and live search query. Server registered in ~/.claude.json per user decision (same location as existing sequential-thinking server).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-agent-integration-test/10-CONTEXT.md
@.planning/phases/10-agent-integration-test/10-RESEARCH.md
@README.md
@src/arxiv_mcp/mcp/server.py
@src/arxiv_mcp/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure MCP server and verify database state</name>
  <files></files>
  <action>
1. Verify database state first:
   - Run `cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && .venv/bin/alembic current` to confirm migration head is 008
   - Run a quick Python script to verify data exists: papers count (expect ~126), interest_profiles (expect 1), triage_states (expect ~98), collections (expect 1), interest_signals (expect ~13)
   - If migrations are behind, run `.venv/bin/alembic upgrade head`

2. Register the MCP server in ~/.claude.json using the Claude Code CLI (per user decision to configure in ~/.claude.json alongside existing servers like sequential-thinking):

   ```bash
   claude mcp add-json arxiv-discovery '{
     "type": "stdio",
     "command": "/home/rookslog/workspace/projects/arxiv-sanity-mcp/.venv/bin/python",
     "args": ["-m", "arxiv_mcp.mcp"],
     "cwd": "/home/rookslog/workspace/projects/arxiv-sanity-mcp",
     "env": {
       "DATABASE_URL": "postgresql+asyncpg://arxiv_mcp:arxiv_mcp_dev@localhost:5432/arxiv_mcp"
     }
   }' --scope local
   ```

   Use absolute venv Python path (not `python`), include `cwd` for pydantic-settings .env discovery, and pass DATABASE_URL explicitly in the env block (belt-and-suspenders per research pitfall 2). The `--scope local` flag writes to ~/.claude.json.

3. Verify the registration succeeded:
   - Run `claude mcp list` and confirm `arxiv-discovery` appears in the output

4. Verify server module loads without errors:
   `/home/rookslog/workspace/projects/arxiv-sanity-mcp/.venv/bin/python -c "from arxiv_mcp.mcp.server import mcp; print('Server module loaded successfully')"`
  </action>
  <verify>
    <automated>.venv/bin/python -c "from arxiv_mcp.mcp.server import mcp; print('OK')" && claude mcp list 2>&1 | grep -q "arxiv-discovery" && echo "Server registered"</automated>
  </verify>
  <done>Database confirmed at migration 008 with Phase 5 data. arxiv-discovery registered in ~/.claude.json via `claude mcp add-json --scope local`. Server module imports cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify MCP server connects in Claude Code</name>
  <files></files>
  <action>
Human verification that the MCP server connects end-to-end in Claude Code. The automated Task 1 registered the server in ~/.claude.json; this checkpoint verifies Claude Code picks it up and can use it to return real data.

Steps:
1. Start a NEW Claude Code session in the arxiv-sanity-mcp project directory (the current session will not pick up config changes)
2. Type `/mcp` to check server status -- arxiv-discovery should appear as Connected
3. Ask: "List the available MCP tools from arxiv-discovery" -- should show 13 tools
4. Ask: "Search for papers about consciousness" -- should return results from the database (not empty)
5. If step 4 returns results, the server is fully connected end-to-end

If any step fails:
- Step 2 (not listed): Run `claude mcp list` from terminal to verify the registration from Task 1 succeeded. If missing, re-run the `claude mcp add-json` command from Task 1.
- Step 3 (no tools): Check for import errors in server startup
- Step 4 (no results): Check DATABASE_URL is pointing to arxiv_mcp (not arxiv_mcp_test)
  </action>
  <verify>
    <automated>echo "Manual checkpoint: user confirms MCP server connects in Claude Code session"</automated>
  </verify>
  <done>MCP server appears as "Connected" in Claude Code. All 13 tools visible. Live search returns papers from production database.</done>
</task>

</tasks>

<verification>
- arxiv-discovery appears in `claude mcp list` output
- Database at migration 008 with Phase 5 data intact
- MCP server connects in Claude Code and returns real search results
</verification>

<success_criteria>
- MCP server appears as "Connected" in Claude Code
- All 13 tools visible to the agent
- Live search query returns papers from the production database
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-integration-test/10-01-SUMMARY.md`
</output>
