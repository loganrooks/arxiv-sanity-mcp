---
phase: 10-agent-integration-test
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - README.md
  - .planning/phases/10-agent-integration-test/10-FRICTION.md
  - src/arxiv_mcp/mcp/tools/discovery.py
  - src/arxiv_mcp/mcp/tools/workflow.py
  - src/arxiv_mcp/mcp/tools/interest.py
  - src/arxiv_mcp/mcp/tools/enrichment.py
  - src/arxiv_mcp/mcp/tools/content.py
autonomous: true
requirements: [SC-4, SC-5]

must_haves:
  truths:
    - "README MCP configuration section matches the working config validated during the session"
    - "Every critical friction point (Blockers) is either fixed with a commit or documented as blocking with a clear explanation"
    - "Every non-critical friction point is tracked in 10-FRICTION.md with v0.2.0 tag"
    - "README installation and setup instructions are complete and accurate (validated by following them)"
  artifacts:
    - path: "README.md"
      provides: "Corrected MCP configuration and setup instructions validated by actual experience"
      contains: "arxiv-discovery"
    - path: ".planning/phases/10-agent-integration-test/10-FRICTION.md"
      provides: "Final friction report with resolution status for all items"
      contains: "Resolution"
  key_links:
    - from: "README.md MCP config block"
      to: ".mcp.json validated config"
      via: "Config instructions match what actually worked"
      pattern: "mcpServers"
    - from: "10-FRICTION.md critical items"
      to: "fix(10): commits"
      via: "Each critical item resolved by a code commit"
      pattern: "fix\\(10\\)"
---

<objective>
Apply critical ergonomic fixes identified during the research session, validate and correct the README setup documentation, and finalize the friction report with resolution statuses.

Purpose: Close the loop on Phase 10 findings. README corrections ensure a new user can actually follow the setup guide. Critical fixes ensure the MCP server works well enough for real agent workflows. Non-critical items are tracked for v0.2.0.

Output: Corrected README, any critical fix commits, finalized friction report.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-agent-integration-test/10-CONTEXT.md
@.planning/phases/10-agent-integration-test/10-FRICTION.md
@.planning/phases/10-agent-integration-test/10-SESSION-LOG.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate README and apply critical fixes</name>
  <files>README.md, src/arxiv_mcp/mcp/tools/discovery.py, src/arxiv_mcp/mcp/tools/workflow.py, src/arxiv_mcp/mcp/tools/interest.py, src/arxiv_mcp/mcp/tools/enrichment.py, src/arxiv_mcp/mcp/tools/content.py</files>
  <action>
**Part A: README Validation and Corrections**

Review the README against the actual experience from the research session (10-SESSION-LOG.md) and fix any issues:

1. **MCP Server Configuration section**: Verify the JSON config block matches the working config from .mcp.json. Key things to check:
   - Does it use absolute venv Python path or generic `python`? (Should note that users need their own venv path)
   - Does it include `cwd`?
   - Does it include `type: "stdio"` explicitly?
   - Does it mention `.mcp.json` as an option for Claude Code project-scoped config?
   - Does it correctly reference `claude_desktop_config.json` for Claude Desktop AND `.mcp.json` or `claude mcp add-json` for Claude Code?

2. **Installation section**: Verify completeness:
   - Does it mention creating a venv? (Currently says `pip install -e .` without venv)
   - Does it mention Python 3.13 requirement prominently?
   - Does it work on a fresh clone?

3. **Quick Start section**: Verify each command works as documented.

4. **Database Setup section**: Verify the steps are correct and in the right order.

Apply corrections to README.md for any issues found. Use clear, user-facing language.

**Part B: Critical Ergonomic Fixes**

Review 10-FRICTION.md for items categorized as Blockers (B-XX) or critical Friction items. For each critical item:

1. Determine if it can be fixed with a small code change (tool description, error message, parameter validation)
2. If fixable: Apply the fix to the relevant tool module(s) in `src/arxiv_mcp/mcp/tools/`
3. If not fixable in this phase: Document why in 10-FRICTION.md and tag as v0.2.0

Commit each fix separately with `fix(10): [description]` prefix.

Known likely fixes (from research/milestone audit -- actual fixes depend on session findings):
- README `"command": "python"` should document venv path guidance
- README may need `.mcp.json` documentation for Claude Code users
- Tool descriptions that caused agent confusion during the session
- Error messages that were unhelpful

NOTE: Only files listed in `files_modified` should be changed. If the session reveals fixes needed in files not listed here (e.g., resource modules, prompt modules), add them to the commit but note the deviation. Do NOT modify test files in this task.
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && .venv/bin/python -m pytest tests/test_mcp/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>README MCP configuration section matches validated working config. All critical friction items either fixed (with commit) or documented with explanation. Non-critical items tagged v0.2.0 in 10-FRICTION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize friction report with resolution statuses</name>
  <files>.planning/phases/10-agent-integration-test/10-FRICTION.md</files>
  <action>
Update 10-FRICTION.md to reflect final resolution status for every item:

1. For each Blocker (B-XX): Add resolution line with either:
   - "Fixed in commit [hash]" (link to the fix(10): commit)
   - "UNRESOLVED -- [reason]. Tracked for v0.2.0"

2. For each Friction item (F-XX): Add resolution line with either:
   - "Fixed in commit [hash]"
   - "Tracked for v0.2.0 -- [brief reason why not fixing now]"

3. For each Ergonomic item (E-XX): Add resolution line:
   - "Tracked for v0.2.0"

4. Add a summary section at the bottom:
```markdown
## Summary

- **Total issues found:** N
- **Blockers:** X (Y fixed, Z unresolved)
- **Friction:** X (Y fixed, Z deferred to v0.2.0)
- **Ergonomic:** X (all v0.2.0)
- **Critical fixes applied:** N commits with fix(10): prefix
```

5. If any known issues from the milestone audit were confirmed during the session (total_estimate: None, find_related_papers lacks workflow enrichment, triage_paper "unseen" confusion), note them in the friction report with their existing documentation status.
  </action>
  <verify>
    <automated>grep -c "Resolution:" .planning/phases/10-agent-integration-test/10-FRICTION.md || grep -c "resolution:" .planning/phases/10-agent-integration-test/10-FRICTION.md || echo "Check friction report for resolution entries"</automated>
  </verify>
  <done>Every friction item has a resolution status. Summary section shows fix counts. All critical items addressed. Report ready for final milestone review.</done>
</task>

</tasks>

<verification>
- README MCP config section uses correct format with venv path guidance
- All MCP tool tests still pass after any description/error message changes
- 10-FRICTION.md has resolution status for every item
- No Blocker items left unresolved without explanation
</verification>

<success_criteria>
- README setup instructions validated and corrected where needed
- All critical ergonomic fixes committed with fix(10): prefix
- All non-critical items tracked for v0.2.0
- MCP test suite still passes
- Friction report finalized with complete resolution statuses
</success_criteria>

<output>
After completion, create `.planning/phases/10-agent-integration-test/10-03-SUMMARY.md`
</output>
