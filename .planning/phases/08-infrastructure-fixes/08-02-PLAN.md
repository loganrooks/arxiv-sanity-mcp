---
phase: 08-infrastructure-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - SC-1

must_haves:
  truths:
    - "Live database schema matches ORM model expectations (composite PK on paper_enrichments)"
    - "enrich_paper CLI command succeeds against real database"
    - "alembic current reports migration 008 as head"
  artifacts:
    - path: "alembic/versions/008_content_variants_table.py"
      provides: "Latest migration (already exists, just needs to be applied)"
  key_links:
    - from: "src/arxiv_mcp/enrichment/service.py"
      to: "paper_enrichments table"
      via: "pg_insert ON CONFLICT on (arxiv_id, source_api)"
      pattern: "on_conflict_do_update.*index_elements.*arxiv_id.*source_api"
---

<objective>
Run pending Alembic migrations (005-008) against the live database to align the schema with code expectations.

Purpose: The live database is stuck on migration 004 (single-column PK on paper_enrichments) while the ORM and service code expect composite PK (arxiv_id, source_api) from migration 006. This blocks enrich_paper against the live database. Migrations 005-008 apply: drop signal type CHECK constraint, add composite PK, add seen triage state, add content_variants table.

Output: Live database at migration 008 with schema matching all ORM models.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-infrastructure-fixes/08-RESEARCH.md

@alembic/versions/005_drop_signal_type_check.py
@alembic/versions/006_enrichment_composite_pk.py
@alembic/versions/007_add_seen_triage_state.py
@alembic/versions/008_content_variants_table.py

<interfaces>
<!-- Migration chain that needs to be applied -->
004 (current) -> 005 (drop signal CHECK) -> 006 (composite PK) -> 007 (seen state) -> 008 (content variants)

<!-- The upsert pattern that requires composite PK -->
From src/arxiv_mcp/enrichment/service.py:
```python
stmt = pg_insert(PaperEnrichment).values(...)
stmt = stmt.on_conflict_do_update(
    index_elements=["arxiv_id", "source_api"],
    ...
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pending Alembic migrations on live database</name>
  <files></files>
  <action>
1. Verify current migration state: `cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && alembic current`
   - Expected output: shows version 004 as current.

2. Verify the target database URL by checking settings (should be the live `arxiv_mcp` database, not `arxiv_mcp_test`):
   ```bash
   python -c "from arxiv_mcp.config import get_settings; s = get_settings(); print(s.database_url)"
   ```

3. Run the migration: `alembic upgrade head`
   - This applies 005, 006, 007, 008 in sequence.
   - Migration 006 changes `paper_enrichments` PK from `(arxiv_id)` to `(arxiv_id, source_api)`. Safe because all existing rows have `source_api = 'openalex'` (server_default) and `arxiv_id` was already unique.

4. Verify final state: `alembic current`
   - Expected: shows version 008 as head.

5. Verify composite PK exists:
   ```bash
   psql -U arxiv_mcp -d arxiv_mcp -c "\d paper_enrichments" | grep -i pkey
   ```
   - Expected: `paper_enrichments_pkey PRIMARY KEY, btree (arxiv_id, source_api)`

6. Verify content_variants table exists:
   ```bash
   psql -U arxiv_mcp -d arxiv_mcp -c "\d content_variants" | head -5
   ```

IMPORTANT: Use the correct conda environment when running alembic. The alembic.ini reads database URL from the application settings.
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && alembic current 2>&1 | grep -q "008" && echo "PASS: at migration 008" || echo "FAIL: not at migration 008"</automated>
  </verify>
  <done>alembic current shows migration 008 as head. paper_enrichments has composite PK (arxiv_id, source_api). content_variants table exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live database migration</name>
  <files></files>
  <action>
Human verifies that the live database migration succeeded and enrichment works against real data.

Steps for verification:
1. Run: `cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && alembic current`
   - Should show migration 008 as current head.
2. Optionally test enrichment against a real paper:
   ```bash
   python -m arxiv_mcp.cli enrich 2301.00001
   ```
   - Should succeed (or fail with "paper not found" if that arxiv_id is not in DB -- but NOT with a schema/constraint error).
3. Verify the full test suite still passes:
   ```bash
   python -m pytest -x -q
   ```
   - Tests use create_all (not alembic), so migration changes should not affect tests.
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && alembic current 2>&1 | grep -q "008" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>User confirms migration 008 applied, enrichment upsert works against live DB without schema errors.</done>
</task>

</tasks>

<verification>
Post-migration verification:
1. `alembic current` shows 008
2. `psql -U arxiv_mcp -d arxiv_mcp -c "SELECT count(*) FROM paper_enrichments"` runs without error
3. Full test suite passes (tests are independent of live DB)
</verification>

<success_criteria>
1. Live database at alembic migration 008 (head)
2. paper_enrichments PK is (arxiv_id, source_api)
3. content_variants table exists
4. seen triage state accepted by CHECK constraint
5. No data loss from migration
</success_criteria>

<output>
After completion, create `.planning/phases/08-infrastructure-fixes/08-02-SUMMARY.md`
</output>
