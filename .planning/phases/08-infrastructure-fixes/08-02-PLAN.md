---
phase: 08-infrastructure-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - SC-1

must_haves:
  truths:
    - "Live database schema matches ORM model expectations (composite PK on paper_enrichments)"
    - "alembic current reports migration 008 as head"
    - "content_variants table exists in the live database"
    - "seen triage state accepted by CHECK constraint in the live database"
  artifacts:
    - path: "alembic/versions/008_content_variants_table.py"
      provides: "Latest migration (already exists, just needs to be applied)"
  key_links:
    - from: "src/arxiv_mcp/enrichment/service.py"
      to: "paper_enrichments table"
      via: "pg_insert ON CONFLICT on (arxiv_id, source_api)"
      pattern: "on_conflict_do_update.*index_elements.*arxiv_id.*source_api"
---

<objective>
Run pending Alembic migrations (005-008) against the live database to align the schema with code expectations.

Purpose: The live database is stuck on migration 004 (single-column PK on paper_enrichments) while the ORM and service code expect composite PK (arxiv_id, source_api) from migration 006. This blocks enrich_paper against the live database. Migrations 005-008 apply: drop signal type CHECK constraint, add composite PK, add seen triage state, add content_variants table.

Output: Live database at migration 008 with schema matching all ORM models.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-infrastructure-fixes/08-RESEARCH.md

@alembic/versions/005_drop_signal_type_check.py
@alembic/versions/006_enrichment_composite_pk.py
@alembic/versions/007_add_seen_triage_state.py
@alembic/versions/008_content_variants_table.py

<interfaces>
<!-- Migration chain that needs to be applied -->
004 (current) -> 005 (drop signal CHECK) -> 006 (composite PK) -> 007 (seen state) -> 008 (content variants)

<!-- The upsert pattern that requires composite PK -->
From src/arxiv_mcp/enrichment/service.py:
```python
stmt = pg_insert(PaperEnrichment).values(...)
stmt = stmt.on_conflict_do_update(
    index_elements=["arxiv_id", "source_api"],
    ...
)
```

<!-- Data safety (from RESEARCH.md) -->
All existing paper_enrichments rows have source_api='openalex' (server_default).
arxiv_id was previously unique PK, so no duplicate (arxiv_id, source_api) pairs can exist.
Migration 005: drops CHECK constraint only (no data risk).
Migration 007: modifies CHECK constraint only (no data risk).
Migration 008: creates new table only (no data risk).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pending Alembic migrations on live database</name>
  <files></files>
  <action>
This task modifies the live database only (no source files). Per the locked SC-1 decision: fully automated, no human checkpoint. Downgrade path exists via `alembic downgrade` as documented escape hatch.

1. **Pre-flight: verify target database URL.** Run:
   ```bash
   cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && python -c "from arxiv_mcp.config import get_settings; s = get_settings(); print(f'Target: {s.database_url}')"
   ```
   Expected output must contain `localhost:5432/arxiv_mcp` (not `arxiv_mcp_test`). If the URL is unexpected, STOP and report.

2. **Verify current migration state:**
   ```bash
   alembic current
   ```
   Expected: shows version 004 as current. If already at 008, this task is a no-op.

3. **Run all pending migrations:**
   ```bash
   alembic upgrade head
   ```
   This applies 005 -> 006 -> 007 -> 008 in sequence:
   - 005: Drop signal_type CHECK constraint (extensible signal types)
   - 006: Change paper_enrichments PK from (arxiv_id) to (arxiv_id, source_api). Safe because all existing rows have source_api='openalex' (server_default) and arxiv_id was already unique.
   - 007: Add 'seen' to triage state CHECK constraint
   - 008: Create content_variants table

4. **Verify final state:**
   ```bash
   alembic current
   ```
   Expected: shows version 008 as head.

5. **Verify composite PK exists:**
   ```bash
   psql -U arxiv_mcp -d arxiv_mcp -c "\d paper_enrichments" 2>/dev/null | grep -i pkey
   ```
   Expected: `paper_enrichments_pkey PRIMARY KEY, btree (arxiv_id, source_api)`

6. **Verify content_variants table exists:**
   ```bash
   psql -U arxiv_mcp -d arxiv_mcp -c "\d content_variants" 2>/dev/null | head -5
   ```

IMPORTANT: Activate the correct conda environment before running alembic. The alembic.ini reads database URL from application settings via get_settings().

**Escape hatch:** If migration fails midway, `alembic downgrade 004` reverts to the pre-migration state.
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && alembic current 2>&1 | grep -q "008" && echo "PASS: at migration 008" || echo "FAIL: not at migration 008"</automated>
  </verify>
  <done>alembic current shows migration 008 as head. paper_enrichments has composite PK (arxiv_id, source_api). content_variants table exists. seen triage state accepted by CHECK constraint. No data loss.</done>
</task>

</tasks>

<verification>
Post-migration verification:
1. `alembic current` shows 008 (head)
2. `psql -U arxiv_mcp -d arxiv_mcp -c "SELECT count(*) FROM paper_enrichments"` runs without error
3. `psql -U arxiv_mcp -d arxiv_mcp -c "SELECT count(*) FROM content_variants"` runs without error (new table, 0 rows expected)
4. Full test suite passes (tests use create_all, independent of live DB)
</verification>

<success_criteria>
1. Live database at alembic migration 008 (head)
2. paper_enrichments PK is (arxiv_id, source_api) -- composite, not single column
3. content_variants table exists
4. seen triage state accepted by CHECK constraint
5. No data loss from migration (paper_enrichments row count unchanged)
</success_criteria>

<output>
After completion, create `.planning/phases/08-infrastructure-fixes/08-02-SUMMARY.md`
</output>
