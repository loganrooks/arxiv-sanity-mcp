---
phase: 05-mcp-validation-iteration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arxiv_mcp/scripts/__init__.py
  - src/arxiv_mcp/scripts/import_arxiv_scan.py
  - tests/test_mcp/test_import.py
autonomous: true
requirements: [MCPV-01]
must_haves:
  truths:
    - "Import script ingests 150 papers from arxiv-scan final-selection.json into the database"
    - "Import script ingests 7 false-negative papers from excluded-paper-audit.json"
    - "Triage states are set from paper-index-data.json value scores (shortlisted >= 7, seen < 7)"
    - "Interest profile is created from the 10 tension vocabulary categories"
    - "Import is idempotent (re-runs do not duplicate data)"
  artifacts:
    - path: "src/arxiv_mcp/scripts/import_arxiv_scan.py"
      provides: "One-time import script composing existing services"
      min_lines: 80
    - path: "src/arxiv_mcp/scripts/__init__.py"
      provides: "Package init"
    - path: "tests/test_mcp/test_import.py"
      provides: "Import script unit tests for data parsing, score mapping, profile creation"
      min_lines: 40
  key_links:
    - from: "src/arxiv_mcp/scripts/import_arxiv_scan.py"
      to: "ArxivAPIClient.fetch_paper"
      via: "service composition for paper ingestion"
      pattern: "ArxivAPIClient|fetch_paper"
    - from: "src/arxiv_mcp/scripts/import_arxiv_scan.py"
      to: "TriageService.mark_triage"
      via: "service composition for triage state setting"
      pattern: "mark_triage"
    - from: "src/arxiv_mcp/scripts/import_arxiv_scan.py"
      to: "ProfileService.create_profile"
      via: "service composition for interest profile creation"
      pattern: "create_profile|add_signal"
---

<objective>
Create an idempotent import script that bootstraps the database with arxiv-scan pipeline data: 150 selected papers + 7 false-negative papers, triage states from value scores, and an interest profile from the tension vocabulary. This provides the real dataset needed for MCP validation in subsequent plans.

Purpose: MCPV-01 requires "at least one real literature review session through MCP." The import script provides the real data for that session. Without imported papers, validation is impossible.
Output: A runnable CLI command (`arxiv-mcp import-scan`) that populates the database with real research data.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mcp-validation-iteration/05-CONTEXT.md
@.planning/phases/05-mcp-validation-iteration/05-RESEARCH.md

@src/arxiv_mcp/mcp/server.py
@src/arxiv_mcp/cli.py
@src/arxiv_mcp/ingestion/arxiv_api.py
@src/arxiv_mcp/ingestion/mapper.py
@src/arxiv_mcp/workflow/triage.py
@src/arxiv_mcp/interest/profiles.py
@src/arxiv_mcp/config.py
@tests/test_mcp/conftest.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/arxiv_mcp/ingestion/arxiv_api.py:
- ArxivAPIClient(settings) -- constructor
- async fetch_paper(arxiv_id: str) -> arXivRaw | None -- fetches single paper
- Rate limiting: 3s between requests (settings.harvest_rate_limit)

From src/arxiv_mcp/ingestion/mapper.py:
- map_to_paper(raw: arXivRaw, source: str) -> Paper -- converts raw to ORM instance

From src/arxiv_mcp/workflow/triage.py:
- TriageService(sf, settings) -- constructor
- async mark_triage(paper_id, new_state, source, reason) -- sets triage state

From src/arxiv_mcp/interest/profiles.py:
- ProfileService(sf, settings) -- constructor
- async create_profile(name, description) -> Profile -- creates interest profile
- async add_signal(profile_slug, signal_type, signal_value, source, reason) -> InterestSignal

From src/arxiv_mcp/cli.py:
- cli = click.group() -- main entry point, subcommands registered via add_command()

Data sources at /scratch/arxiv-scan/pipeline/:
- triage/final-selection.json: {"metadata": {...}, "papers": [{arxiv_id, title, scores, tensions_engaged, normalized_holistic, ...}]}
  - 150 papers, all normalized_holistic >= 7
- reading/paper-index-data.json: [{id, title, value (5-9), tensions, depth, ...}]
  - 150 papers, value 5-9, 109 >= 7 (shortlisted), 41 < 7 (seen)
  - USE THESE VALUE SCORES for triage mapping (not normalized_holistic)
- excluded-paper-audit.json: [{arxiv_id, title, verdict, reasoning, ...}]
  - 7 false-negative papers NOT in final-selection -- must be imported separately
- evaluation-guidelines.md: 10 tension categories with descriptions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Import script with test scaffolding</name>
  <files>src/arxiv_mcp/scripts/__init__.py, src/arxiv_mcp/scripts/import_arxiv_scan.py, tests/test_mcp/test_import.py</files>
  <behavior>
    - Test: load_paper_ids_from_final_selection parses final-selection.json and returns 150 arxiv IDs
    - Test: load_paper_ids_from_excluded_audit parses excluded-paper-audit.json and returns 7 arxiv IDs
    - Test: map_value_to_triage_state returns "shortlisted" for value >= 7, "seen" for value < 7
    - Test: build_tension_signals returns 10 signal dicts (one per tension category) with signal_type="followed_author" (using tension name as proxy signal, since tensions map to topical interest areas)
    - Test: paper IDs from final-selection and excluded-audit do not overlap
  </behavior>
  <action>
Create `src/arxiv_mcp/scripts/__init__.py` (empty package init).

Create `src/arxiv_mcp/scripts/import_arxiv_scan.py` with:

1. **Data loading functions** (pure, testable):
   - `load_final_selection(data_dir: Path) -> list[dict]`: Load final-selection.json, return papers list
   - `load_excluded_papers(data_dir: Path) -> list[dict]`: Load excluded-paper-audit.json, return list
   - `load_paper_index(data_dir: Path) -> dict[str, dict]`: Load paper-index-data.json, return dict keyed by arxiv ID
   - `map_value_to_triage_state(value: int) -> str`: Return "shortlisted" if value >= 7, else "seen"
   - `build_tension_signals() -> list[dict]`: Return list of 10 dicts with keys {signal_type, signal_value, reason} for each tension category from evaluation-guidelines. Use signal_type="followed_author" with the tension name as signal_value (e.g., "autonomy_vs_control"). The reason should include the brief description of each tension.

2. **Async import orchestration** (composes existing services):
   - `async import_arxiv_scan(data_dir: Path, settings)`: Main orchestration function
     - Step 1: Load all data files
     - Step 2: Collect all unique arxiv IDs (150 from final-selection + 7 from excluded-audit)
     - Step 3: Fetch each paper via ArxivAPIClient.fetch_paper() with progress reporting (Rich progress bar). Use sequential fetching respecting rate limit. Idempotent: ON CONFLICT DO NOTHING for upsert.
     - Step 4: Set triage states using paper-index-data.json value scores via TriageService.mark_triage(). For excluded-audit papers (not in paper-index), use "seen" as default.
     - Step 5: Create interest profile "arxiv-scan-tensions" with description from evaluation-guidelines research focus. Add 10 tension signals via ProfileService.add_signal().
     - Step 6: Print summary (papers ingested, triage states set, profile created)

3. **Click CLI command**:
   - `import_scan_group = click.group("import")` with subcommand `scan` accepting `--data-dir` (default: `/scratch/arxiv-scan/pipeline/`)
   - Register in cli.py via `cli.add_command(import_scan_group)`

Create `tests/test_mcp/test_import.py` with tests for all pure data-loading functions. Use inline fixture data (small JSON snippets) rather than reading from /scratch/. Test the score-to-triage mapping exhaustively (values 1-10). Test tension signal generation returns exactly 10 signals.

**Key constraints:**
- Compose existing services (ArxivAPIClient, TriageService, ProfileService) -- do not hand-roll DB operations except for the initial paper upsert (which needs pg_insert ON CONFLICT)
- Use paper-index-data.json value scores for triage mapping, NOT normalized_holistic from final-selection.json
- The 7 excluded-audit papers are not in paper-index-data, so default their triage to "seen"
- Make the import function async; the CLI command wraps it with asyncio.run()
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && python -m pytest tests/test_mcp/test_import.py -x -q</automated>
  </verify>
  <done>
    - Pure data-loading functions tested and passing
    - Score-to-triage mapping tested for full range (1-10)
    - Tension signal builder returns exactly 10 signals
    - Import script registered as CLI command
    - Full test suite passes: `pytest tests/ -x -q`
  </done>
</task>

</tasks>

<verification>
- `pytest tests/test_mcp/test_import.py -x -q` passes (import logic tests)
- `pytest tests/ -x -q` passes (no regressions)
- `python -c "from arxiv_mcp.scripts.import_arxiv_scan import load_final_selection, map_value_to_triage_state, build_tension_signals; print('imports OK')"` succeeds
</verification>

<success_criteria>
- Import script exists with pure data-loading functions and async orchestration
- All data-loading functions have passing unit tests
- CLI command `arxiv-mcp import scan` is registered
- Import is idempotent by design (ON CONFLICT DO NOTHING)
- No new dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/05-mcp-validation-iteration/05-01-SUMMARY.md`
</output>
