---
phase: 06-content-normalization
plan: 04
type: execute
wave: 1
depends_on: ["06-03"]
files_modified: ["pyproject.toml"]
autonomous: true
gap_closure: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, CONT-06, MCP-03]

must_haves:
  truths:
    - "All 176 Phase 6 tests pass (test_content/ + test_mcp/)"
    - "Full test suite passes with no regressions (471+ tests)"
    - "MCP server can start without ModuleNotFoundError"
  artifacts:
    - path: "pyproject.toml"
      provides: "beautifulsoup4 declared as project dependency"
      contains: "beautifulsoup4"
  key_links:
    - from: "pyproject.toml"
      to: "src/arxiv_mcp/content/html_fetcher.py"
      via: "bs4 package installed from declared dependency"
      pattern: "beautifulsoup4"
---

<objective>
Fix missing beautifulsoup4 dependency that blocks all Phase 6 tests and MCP server startup.

Purpose: The VERIFICATION.md identified that `beautifulsoup4` is imported by `html_fetcher.py` but was never added to `pyproject.toml` dependencies. This causes `ModuleNotFoundError: No module named 'bs4'` on any import path that touches `content/html_fetcher.py`, which includes `content/__init__.py`, `content/service.py`, and `mcp/server.py`. All 176 Phase 6 tests are unrunnable and the MCP server cannot start.

Output: Updated pyproject.toml with beautifulsoup4 dependency, venv synced, all tests green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-normalization/06-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add beautifulsoup4 dependency and verify full test suite</name>
  <files>pyproject.toml</files>
  <action>
1. Add `"beautifulsoup4>=4.12"` to the `[project].dependencies` list in `pyproject.toml`. Insert it after the existing `"lxml>=5.3"` line (since beautifulsoup4 uses lxml as its parser backend, keeping them adjacent is logical).

2. Run `uv sync` to install the dependency into the project venv.

3. Run the full test suite: `python -m pytest tests/ -x -q` to confirm all 471+ tests pass (295 pre-Phase-6 + 176 Phase 6 tests).

4. If tests pass, the gap is closed. If any tests fail for reasons unrelated to bs4, investigate and fix.
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && python -m pytest tests/ -x -q</automated>
  </verify>
  <done>beautifulsoup4 is declared in pyproject.toml, installed in venv, and all 471+ tests pass with zero failures.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` passes all tests (471+)
- `python -c "from arxiv_mcp.content.html_fetcher import fetch_arxiv_html; print('OK')"` succeeds
- `python -c "from arxiv_mcp.mcp.server import mcp; print('OK')"` succeeds (MCP server can import)
- `grep beautifulsoup4 pyproject.toml` returns a match
</verification>

<success_criteria>
- beautifulsoup4>=4.12 declared in pyproject.toml [project].dependencies
- All Phase 6 tests runnable and passing (176 tests in test_content/ + test_mcp/)
- Full test suite green with no regressions (471+ total)
- MCP server importable without ModuleNotFoundError
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-normalization/06-04-SUMMARY.md`
</output>
