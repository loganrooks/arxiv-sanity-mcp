---
phase: 01-metadata-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .env.example
  - alembic.ini
  - alembic/env.py
  - alembic/versions/001_initial_schema.py
  - src/arxiv_mcp/__init__.py
  - src/arxiv_mcp/config.py
  - src/arxiv_mcp/cli.py
  - src/arxiv_mcp/db/__init__.py
  - src/arxiv_mcp/db/engine.py
  - src/arxiv_mcp/db/models.py
  - src/arxiv_mcp/models/__init__.py
  - src/arxiv_mcp/models/paper.py
  - src/arxiv_mcp/models/pagination.py
  - data/categories.toml
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_models/__init__.py
  - tests/test_models/test_paper.py
  - tests/test_models/test_pagination.py
autonomous: true
requirements:
  - PAPR-01
  - PAPR-02
  - PAPR-03
  - PAPR-04
  - INGS-03
  - INGS-04

must_haves:
  truths:
    - "Paper model stores arXiv ID as primary key with all required metadata fields"
    - "Four distinct time semantics (submitted, updated, announced, OAI datestamp) are separate columns with correct types"
    - "Per-paper license URI is stored"
    - "Provenance fields (source, fetched_at, processing_tier, promotion_reason) exist on every paper"
    - "External ID columns (DOI, OpenAlex, Semantic Scholar) are nullable and present"
    - "tsvector search column with weighted trigger is created by migration"
    - "Pydantic schemas validate paper data and cursor pagination tokens"
    - "Database migration runs successfully and creates all tables/indexes/triggers"
  artifacts:
    - path: "src/arxiv_mcp/db/models.py"
      provides: "SQLAlchemy Paper model with all columns, indexes, tsvector trigger"
      min_lines: 80
    - path: "src/arxiv_mcp/models/paper.py"
      provides: "Pydantic schemas for Paper, PaperVersion, SearchResult, PaperDetail"
      min_lines: 60
    - path: "src/arxiv_mcp/models/pagination.py"
      provides: "Cursor encode/decode, PageInfo schema"
      min_lines: 30
    - path: "src/arxiv_mcp/config.py"
      provides: "Pydantic Settings for DB URL, arXiv config, category loading"
      min_lines: 30
    - path: "src/arxiv_mcp/db/engine.py"
      provides: "Async engine factory, session factory"
      min_lines: 20
    - path: "alembic/versions/001_initial_schema.py"
      provides: "Migration creating papers table, indexes, tsvector trigger"
      min_lines: 50
    - path: "data/categories.toml"
      provides: "Default arXiv category configuration"
      min_lines: 10
    - path: "tests/conftest.py"
      provides: "Test DB fixtures, sample paper factories, async session fixture"
      min_lines: 40
  key_links:
    - from: "src/arxiv_mcp/db/models.py"
      to: "src/arxiv_mcp/db/engine.py"
      via: "Base declarative import"
      pattern: "from.*engine.*import|from.*models.*import.*Base"
    - from: "src/arxiv_mcp/config.py"
      to: "data/categories.toml"
      via: "Category config loading"
      pattern: "categories.*toml|load.*categories"
    - from: "alembic/env.py"
      to: "src/arxiv_mcp/db/models.py"
      via: "target_metadata for autogenerate"
      pattern: "target_metadata.*=.*Base\\.metadata"
---

<objective>
Foundation: project scaffolding, database schema, paper model, test infrastructure

Purpose: Establish the project skeleton, database layer, canonical paper model with all four time semantics, and test infrastructure that all subsequent plans build on. This is Wave 0 + the paper model in one plan.

Output: Working Python project with uv, SQLAlchemy models, Alembic migration that creates the papers table with tsvector trigger and GIN index, Pydantic schemas for data validation, and a test suite that validates the paper model.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-metadata-substrate/01-CONTEXT.md
@.planning/phases/01-metadata-substrate/01-RESEARCH.md
@.planning/phases/01-metadata-substrate/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffolding and configuration</name>
  <files>
    pyproject.toml, .python-version, .env.example, alembic.ini, alembic/env.py,
    src/arxiv_mcp/__init__.py, src/arxiv_mcp/config.py, src/arxiv_mcp/cli.py,
    src/arxiv_mcp/db/__init__.py, src/arxiv_mcp/db/engine.py,
    src/arxiv_mcp/models/__init__.py, data/categories.toml,
    src/arxiv_mcp/ingestion/__init__.py, src/arxiv_mcp/search/__init__.py
  </files>
  <action>
    Initialize the project using `uv init --lib` (or manually create pyproject.toml with `[project]` and `[build-system]`). Set Python version to 3.13.

    **pyproject.toml**: Add all dependencies from research:
    - Runtime: sqlalchemy[asyncio], asyncpg, alembic, pydantic, pydantic-settings, oaipmh-scythe, lxml, httpx, click, structlog, rich, python-dotenv
    - Dev: pytest, pytest-asyncio, pytest-cov, ruff, mypy
    - Add `[tool.pytest.ini_options]` with `asyncio_mode = "auto"` and `testpaths = ["tests"]`
    - Add CLI entry point: `[project.scripts]` with `arxiv-mcp = "arxiv_mcp.cli:cli"`

    **config.py**: Create Pydantic Settings class `Settings` with:
    - `database_url: str` (default: `postgresql+asyncpg://localhost:5432/arxiv_mcp`)
    - `test_database_url: str` (default: `postgresql+asyncpg://localhost:5432/arxiv_mcp_test`)
    - `arxiv_oai_url: str = "https://oaipmh.arxiv.org/oai"` (new URL per research pitfall 1)
    - `arxiv_api_url: str = "http://export.arxiv.org/api/query"`
    - `categories_file: str = "data/categories.toml"` (resolved relative to project root)
    - `harvest_rate_limit: float = 3.0` (seconds between requests per pitfall 6)
    - `default_page_size: int = 20`
    - `max_page_size: int = 100`
    - Load from .env file via `model_config = SettingsConfigDict(env_file=".env")`
    - Add method `load_categories() -> dict` that reads categories.toml and returns configured category sets

    **.env.example**: Template with DATABASE_URL, TEST_DATABASE_URL, commented explanations.

    **data/categories.toml**: Default category configuration inspired by arxiv-sanity's scope:
    ```toml
    [defaults]
    archives = ["cs", "stat"]

    [categories]
    cs = ["cs.AI", "cs.CL", "cs.CV", "cs.LG", "cs.NE", "cs.IR", "cs.MA", "cs.RO"]
    stat = ["stat.ML", "stat.TH"]
    math = ["math.OC", "math.ST"]
    eess = ["eess.AS", "eess.IV", "eess.SP"]
    ```
    Include comments explaining that this mirrors arxiv-sanity's original scope and categories are configurable.

    **db/engine.py**: Create async engine factory and session factory:
    - `create_engine(url: str) -> AsyncEngine` using `create_async_engine` with pool settings
    - `async_session_factory(engine: AsyncEngine) -> async_sessionmaker`
    - `get_session()` async context manager yielding sessions

    **cli.py**: Create Click group skeleton:
    ```python
    @click.group()
    def cli():
        """arxiv-mcp: arXiv research discovery substrate"""
        pass
    ```
    Import and register subgroups (harvest, search) -- these will be added by Plans 02 and 03. For now, just the main group.

    **alembic.ini + alembic/env.py**: Initialize Alembic. Configure env.py to:
    - Import `Base.metadata` from `arxiv_mcp.db.models`
    - Use `config.Settings().database_url` for the connection URL
    - Support async migrations using `run_async_migrations` pattern from Alembic docs
    - Set `target_metadata = Base.metadata`

    Create empty `__init__.py` files in all package directories: `src/arxiv_mcp/`, `src/arxiv_mcp/db/`, `src/arxiv_mcp/models/`, `src/arxiv_mcp/ingestion/`, `src/arxiv_mcp/search/`.

    Run `uv sync` to install all dependencies.
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && uv run python -c "from arxiv_mcp.config import Settings; s = Settings(); print(f'DB: {s.database_url}'); print(f'OAI: {s.arxiv_oai_url}')"</automated>
  </verify>
  <done>
    - pyproject.toml exists with all dependencies and pytest config
    - `uv sync` succeeds, all packages installed
    - Settings loads with defaults
    - Categories TOML file exists with arxiv-sanity default scope
    - Alembic is configured with async migration support
    - CLI entry point exists (`arxiv-mcp --help` works)
    - All package __init__.py files exist
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLAlchemy Paper model, Alembic migration, and tsvector trigger</name>
  <files>
    src/arxiv_mcp/db/models.py, alembic/versions/001_initial_schema.py,
    tests/conftest.py, tests/__init__.py,
    tests/test_models/__init__.py, tests/test_models/test_paper.py
  </files>
  <behavior>
    - test_canonical_model: Paper model has arxiv_id as primary key (String(20)), is insertable and retrievable
    - test_full_metadata: Paper stores title, authors_text, abstract, categories, primary_category, category_list (ARRAY), submitter, comments, journal_ref, report_no, latest_version, version_history (JSONB)
    - test_external_ids: Paper has nullable doi, openalex_id, semantic_scholar_id columns
    - test_time_semantics: Paper has submitted_date (DateTime TZ), updated_date (DateTime TZ nullable), announced_date (Date nullable), oai_datestamp (Date)
    - test_license: Paper has license_uri column (nullable String)
    - test_provenance: Paper has source (String, default "oai_pmh"), fetched_at (DateTime TZ), last_metadata_update (DateTime TZ nullable), processing_tier (Integer, default 0), promotion_reason (nullable String)
    - test_search_vector_trigger: After inserting a paper with title/authors/abstract, the search_vector column is populated by the trigger (not NULL)
    - test_processing_tier_enum: ProcessingTier IntEnum has values METADATA_ONLY=0, FTS_INDEXED=1, ENRICHED=2, EMBEDDED=3, CONTENT_PARSED=4
  </behavior>
  <action>
    **db/models.py**: Create the Paper SQLAlchemy model exactly as specified in 01-RESEARCH.md Example 2, with these specific details:
    - `Base` from `DeclarativeBase`
    - `ProcessingTier(enum.IntEnum)` with five levels (0-4)
    - `Paper(Base)` with `__tablename__ = "papers"`
    - arxiv_id as String(20) primary key
    - All columns from research: title, authors_text, abstract, submitter, comments, journal_ref, report_no, categories (Text), primary_category (String(20)), category_list (ARRAY(String)), msc_class, acm_class, doi, openalex_id, semantic_scholar_id
    - Four time columns: submitted_date, updated_date, announced_date, oai_datestamp
    - license_uri, latest_version, version_history (JSONB)
    - processing_tier (default 0), promotion_reason
    - source (default "oai_pmh"), fetched_at, last_metadata_update
    - search_vector (TSVECTOR, nullable)
    - `__table_args__` with all indexes from research (GIN on search_vector, GIN on category_list, B-tree on dates, primary_category, processing_tier)

    **alembic/versions/001_initial_schema.py**: Write this migration MANUALLY (not autogenerated) per research pitfall 5. It must:
    - Create the `papers` table with all columns
    - Create the `papers_search_vector_update()` PostgreSQL trigger function using `setweight` on title (A), authors_text (B), abstract (C) with 'english' config for title/abstract and 'simple' for authors
    - Create the `papers_search_vector_trigger` BEFORE INSERT OR UPDATE trigger
    - Create all indexes (GIN on search_vector, GIN on category_list, B-tree on dates, primary_category, processing_tier)
    - The `downgrade()` must drop the trigger, function, indexes, and table

    **tests/conftest.py**: Create shared test fixtures:
    - `test_engine` fixture: creates async engine pointing to test database (from Settings.test_database_url)
    - `test_session` fixture: creates tables via `Base.metadata.create_all`, yields session, drops tables after. This runs the trigger creation SQL directly (since create_all doesn't run Alembic migrations, the trigger must be created manually in tests via `session.execute(text(...))`)
    - `sample_paper_data()` factory function returning a dict with all required Paper fields populated with realistic arXiv data (e.g., arxiv_id="2301.00001", title="Attention Is All You Need", etc.)
    - `sample_paper(session)` fixture that inserts a sample paper and returns it
    - Ensure the tsvector trigger SQL is executed during test setup so search_vector is populated

    **tests/test_models/test_paper.py**: Write tests for all behaviors listed above. Each test should:
    - Insert a Paper using the sample data factory
    - Query it back and assert the relevant fields
    - For test_search_vector_trigger: insert paper, query back, assert search_vector is not None
    - For test_processing_tier_enum: just test the enum values directly (no DB needed)

    Run tests: `uv run pytest tests/test_models/test_paper.py -x -v`
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && uv run pytest tests/test_models/test_paper.py -x -v --timeout=30</automated>
  </verify>
  <done>
    - Paper model has all 25+ columns with correct types
    - ProcessingTier enum has 5 levels
    - Migration file creates table, trigger function, trigger, and all indexes
    - All 8 test cases pass
    - search_vector is populated by trigger after insert
    - Tests use isolated test database, create/drop tables per session
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pydantic schemas for paper data and cursor pagination</name>
  <files>
    src/arxiv_mcp/models/paper.py, src/arxiv_mcp/models/pagination.py,
    src/arxiv_mcp/models/__init__.py,
    tests/test_models/test_pagination.py
  </files>
  <behavior>
    - test_paper_version_schema: PaperVersion validates version string, date, optional size/source_type
    - test_paper_summary_schema: PaperSummary has all "rich result" fields: arxiv_id, title, authors_text, abstract_snippet (truncated), categories, primary_category, submitted_date, updated_date, announced_date, oai_datestamp, latest_version, license_uri
    - test_paper_detail_schema: PaperDetail extends PaperSummary with full abstract, version_history, doi, comments, journal_ref, source, fetched_at, processing_tier
    - test_search_result_schema: SearchResult wraps PaperSummary with score (optional float)
    - test_cursor_encode_decode: Cursor(sort_value, paper_id) round-trips through base64 encode/decode
    - test_cursor_invalid_token: Cursor.decode with garbage input raises ValueError
    - test_page_info: PageInfo has has_next (bool), next_cursor (optional str), total_estimate (optional int)
    - test_paginated_response: PaginatedResponse is generic over item type, contains items list + page_info
  </behavior>
  <action>
    **models/paper.py**: Create Pydantic v2 schemas:
    - `PaperVersion(BaseModel)`: version (str), date (datetime), size (str | None), source_type (str | None)
    - `PaperSummary(BaseModel)`: All "rich result" fields per user decision — arxiv_id, title, authors_text, abstract_snippet (str, max 300 chars), categories (str), primary_category, category_list (list[str]), submitted_date, updated_date, announced_date, oai_datestamp, latest_version (int | None), license_uri (str | None). Include `model_config = ConfigDict(from_attributes=True)` for SQLAlchemy ORM conversion.
    - `PaperDetail(PaperSummary)`: Extends with full abstract, version_history (list[PaperVersion] | None), doi, comments, journal_ref, report_no, source, fetched_at, processing_tier, promotion_reason, openalex_id, semantic_scholar_id
    - `SearchResult(BaseModel)`: paper (PaperSummary), score (float | None)
    - Add a `@classmethod from_orm_paper(cls, paper, abstract_max=300)` on PaperSummary that truncates abstract to abstract_snippet

    **models/pagination.py**: Create cursor and pagination schemas:
    - `Cursor` dataclass with sort_value (str) and paper_id (str), encode() -> str (base64 URL-safe), decode(token) -> Cursor classmethod (with ValueError on invalid). Use the pattern from research Example 2.
    - `PageInfo(BaseModel)`: has_next (bool), next_cursor (str | None), total_estimate (int | None)
    - `PaginatedResponse(BaseModel, Generic[T])`: items (list[T]), page_info (PageInfo)

    **models/__init__.py**: Re-export all public schemas.

    **tests/test_models/test_pagination.py**: Test cursor encode/decode round-trip, invalid token handling, PageInfo construction, PaginatedResponse with typed items.
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && uv run pytest tests/test_models/ -x -v --timeout=30</automated>
  </verify>
  <done>
    - PaperSummary, PaperDetail, PaperVersion, SearchResult schemas exist with all fields
    - PaperSummary has from_attributes config for ORM conversion
    - Cursor encodes to base64 and decodes back with tiebreaker paper_id
    - PaginatedResponse is generic and works with SearchResult items
    - All pagination tests pass
    - models/__init__.py re-exports all schemas
  </done>
</task>

</tasks>

<verification>
All tests in tests/test_models/ pass:
```bash
uv run pytest tests/test_models/ -v --timeout=30
```

Project imports work:
```bash
uv run python -c "from arxiv_mcp.db.models import Paper, ProcessingTier; from arxiv_mcp.models import PaperSummary, PaperDetail, Cursor, PaginatedResponse; print('All imports OK')"
```

Migration file exists and has correct structure:
```bash
ls alembic/versions/001_initial_schema.py
```
</verification>

<success_criteria>
- Project initializes with `uv sync` and all dependencies install
- Paper SQLAlchemy model has all columns from research spec (identity, metadata, classification, external IDs, time semantics, rights, versions, processing tier, provenance, search vector)
- Migration creates papers table with tsvector trigger and all indexes
- Pydantic schemas validate paper data with from_attributes ORM mode
- Cursor pagination encodes/decodes reliably
- All tests pass against test PostgreSQL database
- Categories are configurable via TOML file, not hardcoded
</success_criteria>

<output>
After completion, create `.planning/phases/01-metadata-substrate/01-01-SUMMARY.md`
</output>
