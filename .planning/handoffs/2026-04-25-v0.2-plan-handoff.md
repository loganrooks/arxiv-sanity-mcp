---
type: context-handoff
status: active
date: 2026-04-25
purpose: |
  Self-contained handoff at v0.2 plan commit. Future Claude / Logan reading
  this should be able to dispatch the paired audit (cross-vendor + same-
  vendor adversarial) of the v0.2 plan / VISION / LONG-ARC / METHODOLOGY,
  investigate open spike-program questions, and synthesize findings into
  v0.2 plan revisions before Phase 12 execution begins. This handoff
  replaces the conversation history; do not assume anything in chat memory.
audience: Future Claude (or human reader) with no conversation context
read_first_after_this: see "Files to reopen first" section
predecessor_handoff: 2026-04-25-arxiv-mcp-multi-lens-redirection.md
---

# v0.2 Plan Handoff — Multi-Lens Substrate Plan Committed, Paired Audit Pending

## TL;DR

The 2026-04-25 multi-lens redirection (committed in [ADR-0005](../../docs/adrs/ADR-0005-multi-lens-v0.2-substrate.md)) has been operationalized into a v0.2 plan: 6 phases (12-17), 15 plans, 17 new requirement codes, ~2 months estimated. ADR-0005, VISION.md, LONG-ARC.md, an extended `spikes/METHODOLOGY.md`, and the v0.2 milestone artifacts (`milestones/v0.2-MILESTONE.md`, `ROADMAP.md` extension, `REQUIREMENTS.md` v0.2 section) are committed and pushed. The Phase 3 property audit (Opus rerun) gates Option B selection. `008` is superseded; `007` carries an override annotation for the suite-contract tiebreaker reversal surfaced by the paired adversarial review.

**Immediate next action: dispatch the paired audit** (cross-vendor at high effort + same-vendor adversarial at high effort) of three artifacts together — v0.2 plan / VISION / LONG-ARC. Findings synthesize into plan revisions before Phase 12 execution. The audit setup (paired-review package, prompts, artifacts index, output paths) is laid out below in "Paired audit setup."

**Two open question groups deferred to the next agent**: (a) where v0.2 should potentially have spikes (lens protocol, OpenAlex coverage, citation lens design, profile elicitation, lens-disagreement UX, methodological taxonomy, citation source comparison); (b) the disposition of the existing spike program (005/006 reframings? continuation under different framing? new spikes' artifact location?). Investigate after paired audit findings are in, since audit findings may shift the spike landscape.

## Why this handoff exists

The 2026-04-25 session executed a substantive turn from "redirection committed" to "v0.2 plan committed." That work generated five commits and substantive new artifacts. Rather than leaving the paired audit + open spike questions in chat memory, this handoff captures:

1. What got done this session, anchored to commits.
2. What's decided, what's active, what's open, what's parked.
3. The paired audit setup (scope, dispatch, output paths) so the next agent can dispatch without reconstruction.
4. The open spike questions as a catalogue, flagged for review but not pre-decided.
5. User corrections that should bind subsequent work.

After reading this handoff and the files in "Files to reopen first," future Claude should be able to continue without needing the prior conversation.

## What got done this session

Five commits on `spike/001-volume-filtering`, pushed to origin:

1. **`601b5fc docs(spikes): record methodology audit cycle (pressure pass + paired review)`** — methodology audit cycle pre-redirection (deliberation addendum + review + pressure pass + paired review package + cross-vendor / Opus adversarial outputs).

2. **`52b9d48 docs(deliberation): record multi-lens redirection inflection`** — seven 2026-04-25 deliberations (long-arc/multi-lens, audience reframe, load-bearing assumptions, mediation-vs-position, pass-fail-vs-nuance, pressure-artifacts-before-remedy, recording-deliberations-extensively) + the cross-session redirection handoff.

3. **`4263018 docs(audits): record Phase 3 lens-extensibility property audit`** — first audit (provisional, default Explore agent, model unverified) + Opus 4.7 rerun (verified). Verdicts: Property 1 extensible (signal_type CHECK dropped in alembic 005), Property 2 coupled (no lens param, find_related_papers has no profile awareness), Property 3 coupled (only tsvector indexes; PaperEnrichment.related_works is write-once JSONB). Verifies Option B selection on epistemic grounds.

4. **`3f8e46f docs(adr): commit v0.2 multi-lens substrate (ADR-0005)`** — architectural commitment for v0.2 + STATE.md + PROJECT.md key-decisions row.

5. **`7e8b990 docs(planning): add VISION, LONG-ARC, methodology practice disciplines`** — VISION.md (product identity, lens trajectory, anti-vision, AI-research audience, reopen conditions) + LONG-ARC.md (planning doctrine, protected seams, anti-patterns, brief methodology with cross-reference) + extended `spikes/METHODOLOGY.md` with six practice disciplines from the methodology audit cycle (paired review, model verification before delegating audits, single-reader factual claim verification, calibrated language as default register, pressure-test artifacts before remedies, pattern-watch with self-application).

6. **`bfe73a1 docs(spikes): close methodology audit cycle (007 override + 008 supersession)`** — append override annotation to `007/HANDOFF.md` registering tiebreaker reversal; mark `008/DESIGN.md` superseded with status header; create `008/SUPERSESSION.md` explaining what's preserved (function-in-use question, selected-vs-contributed distinction, pre-registration discipline) and what's discarded (tournament framing, two-challenger cap, embedding-model-winner-picking).

7. **`64102c8 docs(planning): commit v0.2 multi-lens substrate plan`** — ROADMAP.md extension (Phases 12-17 with full goals, success criteria, plan titles), v0.2-MILESTONE.md (prospective scope spec), REQUIREMENTS.md v0.2 section (17 new codes: 5 LENS + 4 CITE + 3 LDIS + 3 LPILOT + 2 MCP, with traceability mapping).

(Memory file `feedback_no_explore_for_audits.md` also written this session: don't delegate audits to default Explore agent; use Opus 4.7 directly or pass `model: "opus"` explicitly. The 2026-04-25 Property audit was first dispatched to a default Explore which missed alembic migration 005 superseding migration 003's CHECK constraint, reaching a wrong verdict; Opus rerun fixed it.)

## Active control surface

- **Project root:** `/home/rookslog/workspace/projects/arxiv-sanity-mcp/`
- **Branch:** `spike/001-volume-filtering` (already up to date with `origin/spike/001-volume-filtering` after `git push`)
- **Main branch:** `main` (no PR open for v0.2 work)
- **Worktrees:** none
- **Parallel workers:** none
- **Latest pushed checkpoint:** `64102c8 docs(planning): commit v0.2 multi-lens substrate plan`
- **Uncommitted state:** `.planning/config.json` (GSD tooling drift, intentionally skipped); `.planning/measurement/` (untracked telemetry, intentionally skipped). No session-work uncommitted.
- **Working tree clean for session work:** yes.

## Files to reopen first (in order)

After this handoff, read in order:

1. `CLAUDE.md` — project rules; binding.
2. `.planning/STATE.md` — current state (`v0.2-defined-planning-pending`).
3. `.planning/VISION.md` — product identity at maturity (canonical, post-redirection distillation).
4. `.planning/LONG-ARC.md` — planning doctrine; protected seams, anti-patterns, methodology brief.
5. `.planning/milestones/v0.2-MILESTONE.md` — v0.2 scope spec.
6. `.planning/ROADMAP.md` — v0.1 frozen + v0.2 active phases (12-17 with full details).
7. `.planning/REQUIREMENTS.md` — v0.2 section (LENS / CITE / LDIS / LPILOT / MCP-08 / MCP-09).
8. `docs/adrs/ADR-0005-multi-lens-v0.2-substrate.md` — architectural commit.
9. `.planning/audits/2026-04-25-phase-3-property-audit-opus.md` — gating audit for Option B.
10. `.planning/spikes/METHODOLOGY.md` — interpretive lenses + practice disciplines (extended).
11. Predecessor handoff: `2026-04-25-arxiv-mcp-multi-lens-redirection.md` — context for the redirection that produced this work.

Optionally also useful:
- `docs/adrs/ADR-0001-exploration-first.md` — the load-bearing ADR.
- `.planning/deliberations/2026-04-25-long-arc-and-multi-lens-redirection.md` — journey artifact.
- `.planning/spikes/008-function-in-use-and-blind-spots/SUPERSESSION.md` — supersession rationale.
- `.planning/spikes/007-training-data-mechanism-probes/HANDOFF.md` — see "Override Annotation" section appended this session.

## State distinctions

### Decided this session

- **v0.2 plan committed.** ADR-0005 + 6 phases (12-17) + 15 plans + 17 requirement codes.
- **Option B chosen on epistemic grounds**, not just cost grounds: shipping at least two lenses validates the abstraction by use, not by design alone. (The Opus property audit reframed the rationale; see audit memo's "Implications for Option A/B/C.")
- **Citation source: OpenAlex first.** Semantic Scholar deferred to v0.3 as a second adapter. OpenAlex coverage spike inline in Phase 14 plan 1; if coverage is too thin, contingency branches (accept thin / pull SS into v0.2 / defer citation lens to v0.3) need to be pre-registered in plan-level work.
- **Pilot scope: Logan only for v0.2.** v0.3 broadens contingent on v0.2 closing cleanly.
- **`SearchResult` backward compat: hide new shape behind default.** Legacy `score` and `ranking_explanation` keep current meaning; `lens_scores` and `per_lens_explanations` are additive opt-in.
- **`007` override annotation appended.** Documentation hygiene for the tiebreaker reversal; operationally moot given `008` supersession.
- **`008` superseded.** Tournament framing dissolved; function-in-use evaluation surface migrates to v0.2 Phase 17 (longitudinal pilot harness).
- **Phase 6 (content normalization) pull-forward: not needed.** Already complete in v0.1; citation lens does not depend on full-text content.

### Active (in flight)

- **Paired audit pending dispatch.** Cross-vendor at high effort + same-vendor adversarial at high effort, scope: v0.2 plan + VISION + LONG-ARC + METHODOLOGY. Dispatched after this handoff is committed. See "Paired audit setup" below.

### Open — for next agent to investigate

- **Where v0.2 should potentially have spikes.** Catalogue below; not pre-decided. After paired audit findings are in, the next agent should investigate whether any of these warrant pre-Phase-12 spike work or pre-phase-PLAN-level spike work, or none.
- **Spike program disposition.** What happens to 005, 006 (which are "complete" per their handoffs but were operating under the tournament-narrowing frame the redirection has displaced)? Does the spike program continue under a new frame, formally close, or get re-scoped? Where do new v0.2-related spikes (if any) live in the artifact tree?
- **Phase-PLAN-level pressure-tests.** When Phase 12's PLAN.md is drafted (via `/gsd:plan-phase` or equivalent), apply the same paired-review discipline before execution. Same for Phase 14 (the spike-gated empirical-risk phase).

### Parked (not active, not killed)

- **Tournament-style narrowing of embedding models.** Not abandoned for *one lens* (the semantic lens may still need an embedding-model design decision in v0.3) but no longer load-bearing for v0.2 architecture.
- **Stella, Qwen3, GTE.** Carried-but-deferred at various points in 005-007. Re-enter via the lens-design process if and when relevant; not in v0.2 scope.
- **Methodological-lens taxonomy.** v0.3+ work; out of v0.2 scope per ADR-0005.
- **pgvector / graph-DB backend.** v0.2 uses PostgreSQL with denormalized citation projections; backend swap is open future-shape, not v0.2 work.
- **Behavior-derived profile signals.** v0.3+ per ADR-0005 open scoping.

### Repo-local evidence

- All artifacts under `.planning/` and `docs/`.
- No external evidence (paired audit outputs will be added under `.planning/audits/`).

## Paired audit setup

### Scope

Audit covers — *as a single integrated artifact set* — three documents:
1. `.planning/milestones/v0.2-MILESTONE.md` (scope spec)
2. `.planning/ROADMAP.md` (specifically Phases 12-17 and the v0.2 phase details)
3. `.planning/REQUIREMENTS.md` (specifically the v0.2 Requirements section)
4. `docs/adrs/ADR-0005-multi-lens-v0.2-substrate.md` (architectural commit)
5. `.planning/VISION.md` (product identity)
6. `.planning/LONG-ARC.md` (planning doctrine)
7. `.planning/spikes/METHODOLOGY.md` (specifically the practice-disciplines extension committed this session)

The audit reads these as one artifact set because v0.2's plan rests on the vision/long-arc/methodology choices; auditing them separately misses the integration.

### Effort

**`high`** (per user instruction, not `xhigh`).

### Dispatch protocol

Same protocol as the 2026-04-25 methodology audit cycle's paired review:

- **Cross-vendor primary**: GPT-5.5 via codex CLI, reasoning_effort=high. Dispatched via Bash. Output redirected to `.planning/audits/2026-04-25-v0.2-plan-audit-cross-vendor.md`.
- **Same-vendor adversarial**: Claude Opus 4.7 via Agent tool with explicit `model: "opus"` override. Use `general-purpose` subagent type (has Write tool, unlike Explore). Reasoning effort high (passed via prompt). Output written by the agent to `.planning/audits/2026-04-25-v0.2-plan-audit-opus-adversarial.md`.

Both run in parallel (independent prompts, no shared output).

### Audit prompt structure

Each prompt should ask the reviewer to apply, in order:

1. **LONG-ARC anti-pattern check.** Does the plan re-introduce tournament narrowing, accidental single-lens interface, silent defaults, ADR-violation-by-drift, closure pressure, embedding-model-as-load-bearing, or single-reader framing claims as authoritative?
2. **VISION coherence check.** Does the v0.2 plan honor VISION's load-bearing claims (multi-lens substrate; research-practice operations beyond retrieval; longitudinal-memory commitment; provenance/inspectability; lens trajectory)? Does it honor the anti-vision (not paper chatbot, not RAG wrapper, not ranked-list-but-fancier, not arxiv-sanity-but-better)?
3. **LONG-ARC protected-seams check.** Does the plan preserve all named seams (lens extensibility, bundle-of-signals primitive, per-lens provenance, MCP surface lens-awareness, citation as first-class data, longitudinal state, inspectability, MCP-native operations)?
4. **METHODOLOGY practice-discipline check.** Does the plan apply paired-review discipline, calibrated language, pre-registration of empirical thresholds, model verification, pattern-watch?
5. **Critical-path realism check.** Is `12 → 13 → 16 → 17` realistic? Are 14 and 15 dependencies right? Is the "~2 months" estimate plausible given the dependency graph?
6. **Coverage check.** Does the plan miss anything that ADR-0005 commits or that VISION names as load-bearing?
7. **Risk completeness.** Are the named risks the actual risks? What's missing?

### Paired-review package

Should be assembled at `.planning/audits/2026-04-25-v0.2-paired-audit-package/` (matching the prior session's spike paired-review package pattern):
- `README.md` — package description, methodology binding, dispatch instructions
- `cross-vendor-prompt.md` — full prompt for codex CLI dispatch
- `opus-adversarial-prompt.md` — full prompt for Agent tool dispatch (model=opus)
- `artifacts-index.md` — paths and brief descriptions of every artifact in scope

After both reviews complete, the next agent (or this session, if time permits) writes a synthesis at `.planning/audits/2026-04-25-v0.2-plan-audit-synthesis.md` integrating findings from both reviews and proposing concrete plan revisions. The synthesis is the actionable artifact; the two paired-review outputs are evidence.

## Open spike questions (for next agent investigation)

These are not pre-decided. Investigate after paired audit findings are in (since audit findings may shift the landscape — e.g., if the audit finds the lens protocol design is fragile, that strengthens the case for a pre-Phase-12 design spike).

### A. Lens protocol design spike (pre-Phase 12)

Should a small design spike validate the `Lens` protocol against multiple hypothetical lens types (citation, author, methodological, behavior-derived) before Phase 12 plan 1 commits to the protocol shape? The current plan has the design walkthrough at Phase 15 plan 3, after Phase 12-13-14-15 plans 1-2 have committed. Cost asymmetry: early discovery is cheap; late discovery requires rework across 8+ plans.

### B. OpenAlex coverage spike (pre-Phase 14, gating)

Phase 14 plan 1 *contains* the coverage spike, but the spike's outcome may force pulling Semantic Scholar into v0.2 scope (extending timeline) or deferring citation lens entirely. Should this be a pre-v0.2 spike that gates the citation source decision before the v0.2 plan locks? Or accept it inside Phase 14 with pre-registered contingency branches?

### C. Citation lens design spike (pre-Phase 15)

Citation lens design choices (traversal depth, co-citation neighborhood definition, freshness handling, retrieval cost) could benefit from a spike before Phase 15 implementation. Or roll into Phase 15 plan 1 design work.

### D. Profile-elicitation alternatives spike

The redirection handoff flagged this as "spike value high, when?" Bundle-of-signals primitive is already validated (Property audit Property 1 = extensible), but adding a non-MiniLM-derived signal (e.g., behavior-derived) before Phase 12 might inform the abstraction. Likely defer to v0.3 — but could be a small upfront spike to de-risk.

### E. Lens-disagreement UX spike (pre-Phase 16)

The MCP surface design choice (separate tools vs `mode=` parameter vs composable filter) for lens disagreement / intersection has UX implications for agents. A small spike with one researcher (Logan) testing alternative shapes could inform the choice. Or roll into Phase 16 plan 1 design work.

### F. Methodological lens taxonomy spike

For v0.3 work, but starting now could inform Phase 15 plan 3's design walkthrough. Defer unless the audit surfaces a need.

### G. Citation source comparison spike (OpenAlex vs Semantic Scholar)

Contingent on B revealing OpenAlex coverage is thin. Possibly co-located with B.

## Spike program disposition (for next agent)

Open questions:

- **`005`, `006` reframings.** They're "complete" per their handoffs but were operating under the tournament frame the redirection displaces. Their narrowing decisions are less consequential than `007`'s tiebreaker reversal (which got an override annotation). Do they need similar reframings? Probably not, but the next agent should decide.
- **Continuation vs closure.** Is "the spike program" formally closed at `008` supersession, with new v0.2-related spikes treated as a fresh program? Or does it continue with renumbering (`009`+)? If closed, where does the closure get documented? `.planning/spikes/SPIKE-PROGRAM-CLOSURE.md`?
- **Artifact location for new spikes.** If new spikes run for v0.2 (per A-G above), where do they live? `.planning/spikes/v0.2-A-lens-protocol/` or `.planning/spikes/009-lens-protocol/` or under `.planning/audits/` if they're plan-validation-shaped rather than design-experiment-shaped?
- **Methodology binding.** Any new spike must apply `spikes/METHODOLOGY.md`'s six interpretive lenses + six practice disciplines from the start, not retrofitted. The lessons from the 2026-04-25 audit cycle should be visible in the spike's design.

The next agent's investigation may benefit from delegating to a review agent (general-purpose subagent_type, `model: "opus"`, fresh session, given the v0.2-MILESTONE / VISION / LONG-ARC / METHODOLOGY / paired-audit-findings as input). The review agent's output is a recommendation; the next agent (or Logan) decides.

Logan's prior guidance: "perhaps (and likely) by delegating some kind of review agent, unless you think otherwise or differently." My judgment: delegating spike-question review to a sub-agent is the right move *after* the paired audit findings are in, because the spike landscape depends on what the audit surfaces. Don't dispatch the spike-question review until the paired audit is synthesized.

## Recommendations for next agent

1. **Read this handoff and the files in "Files to reopen first."**
2. **Verify the paired audit's status.** If outputs at `.planning/audits/2026-04-25-v0.2-plan-audit-cross-vendor.md` and `2026-04-25-v0.2-plan-audit-opus-adversarial.md` exist, proceed to step 3. If not, dispatch them per "Paired audit setup" above.
3. **Synthesize paired audit findings** at `.planning/audits/2026-04-25-v0.2-plan-audit-synthesis.md`. Adopt non-framing-claim findings immediately as plan revisions; flag framing-claim findings for further consideration.
4. **Investigate the open spike questions** with synthesized audit findings as context. Likely best to delegate to a review agent (general-purpose, model=opus, given full context).
5. **Decide spike program disposition** based on (3) and (4). Document the disposition.
6. **Apply phase-PLAN-level paired audit discipline** when Phase 12's PLAN.md is drafted (and especially Phase 14 plan 1 since it contains the OpenAlex coverage spike).
7. **Begin Phase 12 execution only after the paired audit is synthesized and plan revisions are committed.** Don't begin execution on a plan with unaddressed audit findings.

## User corrections (this session — these bind subsequent work)

1. **Don't use the default Explore agent for audits.** Audits gating decisions need known-quality models. Use Opus 4.7 directly, or dispatch with explicit `model: "opus"`. The cost test: if a wrong verdict would cost more than half a day of rework or propagate into a roadmap commit, the model must be known. (See `feedback_no_explore_for_audits.md` in memory.)
2. **Audits go to `.planning/audits/`, not `.planning/handoffs/`.** Handoffs are session-transfer artifacts; audits are evidence documents.
3. **Calibrated language as default register, not closing footnote.** Confidence calibration must propagate through the prose where claims are made, not be confined to a closing section.
4. **Don't write a self-audit before the paired review is dispatched.** Single-reader same-session same-author audit is vulnerable to register-blindness and confirmation bias, and biases the paired review if the reviewers see it. Paired audit first; synthesis second.
5. **Don't decide spike questions now; set up the next agent to investigate.** The spike landscape depends on audit findings. The next agent reviews after the audit is synthesized, possibly via delegation.
6. **Audience is AI / CS / ML researchers, not philosophy researchers.** (Carried from the prior session; still binding.)
7. **Take positions; don't mediate-and-synthesize for its own sake.** (Carried from the prior session; still binding.)
8. **Cost of redirection is acceptable.** No code is being torn up. Don't pull punches on architectural questions. (Carried from the prior session; still binding.)

## Sign-off

This handoff is the canonical context-restoration document at v0.2 plan commit. Future Claude reading this fresh should:

1. Read this handoff in full.
2. Read the files in "Files to reopen first" in order.
3. Verify or dispatch the paired audit per "Paired audit setup."
4. Synthesize and adopt; investigate open spike questions; decide spike program disposition.
5. Treat user corrections as binding.

The redirection is decided, the v0.2 plan is committed, the paired audit is the next concrete move, and the spike landscape is open for next-agent investigation. Everything else is downstream.
