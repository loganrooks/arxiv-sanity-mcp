---
type: audit-synthesis
status: revised-2026-04-26-with-dispositions
date: 2026-04-26 (revised)
target: v0.2 plan + VISION + LONG-ARC + METHODOLOGY (integrated artifact set)
inputs:
  - 2026-04-25-v0.2-plan-audit-cross-vendor.md (primary)
  - 2026-04-25-v0.2-plan-audit-opus-adversarial-xhigh.md (primary)
  - 2026-04-25-v0.2-plan-audit-opus-adversarial.md (secondary)
  - 2026-04-25-v0.2-plan-audit-opus-adversarial-xhigh-contaminated.md (secondary)
  - 2026-04-25-v0.2-plan-audit-comparison.md (analysis)
weighting: |
  Primary pair = independent xhigh + cross-vendor (the methodology-compliant
  paired review). Secondary audits (high SV original, contaminated xhigh) are
  weighted lower — used only where they converge with the primary or add
  something that survives independent scrutiny on substance, not on
  prior-anchoring artifact.
purpose: |
  Convert audit findings into concrete plan-revision proposals before Phase 12
  execution starts. Tiered by adoption disposition (immediate edit, deliberate,
  defer, methodology). Each adoption proposal is commit-ready: file:line +
  current text + proposed text + reasoning.
revision_note: |
  Original synthesis (2026-04-26) was drafted with B-tier and C-tier items
  pending Logan's call. This revision (same date) records the dispositions
  made on those items and reorganizes around an A1-A7-then-governance-audit-
  then-rest sequencing. Where the revision narrows or flips the original
  recommendation, the rationale is recorded inline. Flipped: B3 (now defer
  to Phase 14 PLAN.md), C1 (skip the lighter frontmatter pointer too).
  Narrowed: C2 (1 instance, not 4-6), C3 (1 list, not 2). Promoted: D2
  (from defer to adopt-now at LONG-ARC). New section: §8 governance-doc
  audit gap and revised sequencing in §9.
---

# Audit Synthesis — v0.2 Plan Revisions Before Phase 12 (revised)

## 0. Disposition summary (revised)

| Tier | Count | Action |
|---|---:|---|
| **Adopt now (commit-ready edits)** | 7 | A1-A7 — uncontested in primary pair; commit before Phase 12 plan 1 authoring |
| **Adopt at architectural layer with confirmed shape** | 4 | B1-B4 — dispositions resolved this revision |
| **Adopt at register layer with narrowed scope** | 2 | C2 (narrowed to 1 instance), C3 (narrowed to 1 list) |
| **Skip — register inflation without substance** | 1 | C1 — original recommendation flipped this revision |
| **Adopt at doctrine layer (LONG-ARC seam)** | 1 | D2 — promoted from defer this revision |
| **Defer to opportunistic later edit** | 1 | D1 (METHODOLOGY scope-creep edits) |
| **Methodology amendment (Hypothesis)** | 1 | M1 — wording refined; lands before governance audit dispatches |
| **Drop / not adopting** | 2 | N1, N2 — unchanged |

Total: 19 actionable items derived from 25 substantive findings. 16 adopted in some shape; 1 skipped on revision; 1 deferred; 2 dropped; 1 methodology amendment with Hypothesis status.

**Sequencing change vs original synthesis:** the original §7 proposed an 8-step pre-Phase-12 commit checklist with all commits landing in sequence. The revised §9 splits commits into three waves separated by the governance-doc audit. Rationale: the original sequencing did not account for the not-yet-audited governance-doc set (ADRs 0001-0004, AGENTS.md, REQUIREMENTS outside v0.2, ROADMAP outside Phases 12-17, STATE.md, foundation-audit notes, ECOSYSTEM-COMMENTARY). A1-A7 are reconciliations and clarifications that don't depend on doctrinal claims the governance audit could plausibly disturb; B-tier and D2 are doctrine-adjacent and benefit from waiting.

## 1. Adopt now — commit-ready edits

These are concrete edits ready to land in plan-revision commits before Phase 12 execution starts. Each has multi-audit convergence in the primary pair (independent xhigh + cross-vendor) and a defensible single-shape proposal. **Unchanged from the original synthesis.**

### A1. Reconcile `lens=` default across three documents (Finding #1)

**Surface:** `docs/adrs/ADR-0005-multi-lens-v0.2-substrate.md:31`, `.planning/milestones/v0.2-MILESTONE.md:15`, `.planning/REQUIREMENTS.md` MCP-08 (around line 134).

**Current state:** ADR-0005 says "defaulting to a configurable preferred lens or to 'all available'"; MILESTONE says "Default to semantic lens for backward compatibility"; REQUIREMENTS says "default preserves v0.1 behavior."

**Proposed edit (canonical statement at ADR-0005:31):**
> `lens=` parameter on the four discovery MCP tools (`search_papers`, `browse_recent`, `find_related_papers`, `get_paper`). When `lens=` is omitted, behavior is single-lens semantic, preserving v0.1 response shape for backward compatibility. This default is a backward-compat decision flagged for sunset review after Phase 17 pilot signals are interpreted (LPILOT-03 milestone gate). "All available" considered as a UX-richer default and deferred — `v0.1` callers expect single-lens response shape.

**Cross-references:** MILESTONE.md:15 cites the ADR statement rather than restating it; REQUIREMENTS.md MCP-08 cites the ADR statement.

**Reasoning:** Convergent finding across all four audits. The current fork is the silent-defaults anti-pattern (`LONG-ARC.md:48`) operationalized at the document layer. One canonical statement, two cross-references.

### A2. Specify multi-value `lens=` behavior at architectural-commit layer (Finding #2)

**Surface:** `docs/adrs/ADR-0005-multi-lens-v0.2-substrate.md` (extend the `lens=` paragraph at :31).

**Current state:** No document specifies what happens for `lens=["semantic", "citation_community"]` without `mode=`. Phase 16 covers explicit lens-disagreement and intersection ops, but the multi-value form of an ordinary discovery query is unspecified.

**Proposed edit (extension to ADR-0005:31):**
> Multi-value `lens=` (a list of lens names) without an explicit `mode=` parameter returns per-lens result *dict* keyed by lens name: `{"semantic": [...results...], "citation_community": [...results...]}`. This is *not* fusion. Callers requesting fusion must pass `mode="fusion"` explicitly; callers requesting intersection or disagreement use Phase 16's dedicated operations. The default of per-lens dict honors ADR-0001's coexistence commitment at the API contract layer and the LONG-ARC anti-pattern against silent fusion-by-default (`LONG-ARC.md:46`, `:48`).

**Cross-reference:** Add Phase 13 success criterion in ROADMAP.md (around :282-285): "Multi-value `lens=` semantics implemented per ADR-0005's per-lens-dict default; explicit `mode=` parameters route to Phase 16 ops; fusion is opt-in via `mode='fusion'`."

**Reasoning:** Independent xhigh and high SV both flag this as blocking. Independent xhigh frames it as silent-default-by-absence: "When a Phase 12 or 13 implementer hits `search_papers(lens=["semantic", "citation_community"])` for the first time, they will pick a default behavior … that default will become the de facto API." The per-lens-dict default is the most multi-lens-honoring choice — it forces consumers to handle multi-lens results explicitly rather than collapsing them to a single ranked list.

### A3. Multi-attribution capture in Phase 17 (Finding #3 + #20)

**Surface:** `.planning/REQUIREMENTS.md:129` (LPILOT-02), `.planning/ROADMAP.md:353-354` (Phase 17 success criteria).

**Current state:** REQUIREMENTS.md:129: "Harness captures triage events with timestamps: selection, dismissal, return-to-paper after dismissal, lens-of-record per event." ROADMAP.md:353: "per-event lens-of-record."

**Proposed edits:**

REQUIREMENTS.md:129 (LPILOT-02):
> Harness captures triage events with timestamps: selection, dismissal, return-to-paper after dismissal. **For each event, captures the set of lens(es) that surfaced the paper for that event** (multi-valued; never assumes one-lens-per-event), plus the user's explicit lens selection if any (separate field). Multi-lens attribution preserved through analysis — the analysis layer must not reduce events to a single lens-of-record.

ROADMAP.md:353-354 (Phase 17 plan 1 success criteria):
> Capture schema designed: session-level lens usage, query log with lens(es) selected, triage events (selection, dismissal, return-to-paper), per-event multi-lens attribution (set-valued, not singular), durable storage, exportability. **Capture-schema change-control rule: mid-pilot schema additions are allowed; mid-pilot reinterpretation of existing fields is forbidden. Schema is versioned; the end-of-window artifact reports against schema version.**

**Reasoning:** Convergent finding (high SV + both xhigh runs). The "lens-of-record" formulation pre-commits the pilot's analysis to single-lens attribution, which is exactly the tournament framing 008 was superseded for (`MILESTONE.md:30`). The change-control rule (from contaminated xhigh's #20, surviving primary scrutiny) prevents the silent-redefinition failure mode mid-pilot — when the data starts arriving, there will be pressure to "reinterpret" existing fields to answer the questions the analyst wishes had been asked.

### A4. Pre-registered decision matrix for OpenAlex coverage spike (Finding #4)

**Surface:** `.planning/ROADMAP.md:299-303` (Phase 14 plan 1 success criteria), `.planning/milestones/v0.2-MILESTONE.md:79-86` (Risks / Open scoping calls).

**Current state:** ROADMAP.md:299: "OpenAlex coverage spike completes: edge density per paper, freshness profile, missingness pattern characterized for the 126-paper corpus; results recorded in a spike artifact." This is a measurement commitment, not a decision commitment.

**Proposed edit (extension at ROADMAP.md:299):**
> OpenAlex coverage spike completes with **pre-registered decision matrix** governing the storage-shape commitment in Phase 14 plan 2:
>
> - **Outcome A (sufficient):** median paper has ≥K cited-by edges *and* missingness ≤X% on AI/CS papers post-2023. Action: proceed with OpenAlex-only as committed in MILESTONE.
> - **Outcome B (intermediate):** between thresholds A and C. Action: proceed with OpenAlex-only for v0.2 but flag in 14-02 schema design that Semantic Scholar adapter is more likely needed in v0.3 than the current "deferred" framing suggests.
> - **Outcome C (insufficient):** median paper has \<K cited-by edges *or* missingness >X% on AI/CS papers post-2023. Action: re-deliberate citation source choice; pre-registered options are (i) pull Semantic Scholar adapter into v0.2 (extends timeline), (ii) defer citation lens to v0.3, (iii) ship v0.2 with thin citation lens explicitly scoped to direct-citation-only (no co-citation neighborhoods).
>
> K and X are decided in 14-01-PLAN.md *before* the spike runs, with rationale recorded.

**Cross-reference:** MILESTONE.md:79 risk text extended: "Mitigation: pre-registered decision matrix in Phase 14 plan 1 specifies fallback branches before the spike runs (per audit synthesis 2026-04-26)."

**Reasoning:** Convergent across all four audits. METHODOLOGY discipline B and the Bayesian lens (`METHODOLOGY.md:32-40`) require pre-registered priors and decision rules. Without the matrix, the spike's results will be interpreted post-hoc with sunk-cost-bias toward "this is sufficient." Independent xhigh's recommendation also adds a cheap content-presence verification: "Phase 14 plan 1's coverage spike should also verify content presence in `PaperEnrichment.related_works` for the existing 126 papers, not just OpenAlex coverage going forward. This is a 30-second `SELECT COUNT(*) WHERE related_works IS NOT NULL` check that would close the loop." Adopt that as part of 14-01 scope.

### A5. Critical-path correction (Finding #5)

**Surface:** `.planning/milestones/v0.2-MILESTONE.md:65`.

**Current state:** "15 plans total. Critical path: 12 → 13 → 16 → 17 (with 14, 15 in parallel where possible)."

**Proposed edit:**
> 15 plans total. Critical path: `(12 ∥ 14) → 15; 12 → 13; (13 ∧ 15) → 16 → 17`. Phase 12 (Lens Abstraction Primitives) and Phase 14 (Citation Graph Data Integration) parallelize; Phase 15 (Citation/Community Lens) depends on both. Phase 13 (MCP Surface Lens-Awareness) depends on Phase 12 only. Phase 16 (Lens-Disagreement and Intersection Ops) depends on Phase 13 *and* Phase 15. Phase 17 (Longitudinal Pilot Harness) depends on Phase 13, 15, 16.

**Reasoning:** Cross-vendor reviewer's finding; uncontested in the substantive comparison. The previously-stated path was an under-specification; the operational artifact (ROADMAP.md:333, :314) had the actual chain. The "~2 months" estimate rests on the corrected chain, not the previously-stated one.

### A6. Extend backward-compat risk to MCP/CLI/docs (Finding #18)

**Surface:** `.planning/milestones/v0.2-MILESTONE.md:88` (Risks).

**Current state:** "`SearchResult` shape change ripple. Generalization touches all retrieval consumers (CLI, MCP tools, tests). Mitigation: backward-compat default; Phase 12 plan 2 includes regression tests preserving v0.1 response shape."

**Proposed edit:**
> `SearchResult` shape change ripple. Generalization touches all retrieval consumers: **CLI flag interfaces, CLI output formatters, MCP tool signatures, MCP tool docstrings, MCP resource schemas, and the v0.1 test suite (403 tests, of which an audit subset specifically covers retrieval response shape).** Mitigation: backward-compat default per ADR-0005; Phase 12 plan 2 includes regression tests preserving v0.1 response shape; Phase 13 plan 1 includes an audit pass on existing CLI flag and MCP tool docstring surfaces, with an explicit list of any user-facing wording changes recorded as part of the phase artifact.

**Reasoning:** Cross-vendor's coverage finding; the same-vendor audits all read the risk at the architectural-claim layer and missed the consumer-impact layer.

### A7. Convert tournament-narrowing-pattern-watch into a Phase boundary checkpoint (Finding #19)

**Surface:** `.planning/milestones/v0.2-MILESTONE.md:91` (Risks); add success criterion to ROADMAP Phase 15 plan 3 or Phase 16 plan 1.

**Current state:** MILESTONE.md:91: "Tournament-narrowing pattern recurrence … Mitigation: LONG-ARC.md anti-pattern watch; rank-and-deprioritize framing throughout."

**Proposed edits:**

MILESTONE.md:91 risk text:
> Tournament-narrowing pattern recurrence. The redirection is committed but the pattern is recurrent; Phase 12-15 work could quietly drift toward "which lens wins" if not actively counter-postured. Mitigation: LONG-ARC.md anti-pattern watch *operationalized* at Phase 15 plan 3 (lens-extensibility design walkthrough) and Phase 16 plan 1 (lens-disagreement API design) — see ROADMAP success criteria for both.

ROADMAP.md Phase 15 plan 3 success criterion:
> Adds explicit anti-pattern check: "the lens-extensibility walkthrough verifies that no scoring/aggregation step in the implemented substrate has a single-lens-as-default behavior; per-lens framing is preserved at the *implementation* layer, not just the interface layer. If the check finds tournament-narrowing structure, the walkthrough surfaces it as a finding to address before Phase 16."

ROADMAP.md Phase 16 plan 1 success criterion:
> Adds explicit anti-pattern check: "no scoring fusion is the implicit default in any new MCP path; pattern explicitly checked at this phase boundary; if any code path defaults to fusion without explicit `mode='fusion'`, the path is flagged as architectural debt and added to the synthesis backlog."

**Reasoning:** Independent xhigh's net-new finding (#19). The team's diagnosis from the 005-008 cycle was that *vigilance was what failed*; mitigating recurrence with vigilance recapitulates the failure mode. Converting to phase-boundary checkpoints is the structural counter-posture.

## 2. Adopted at architectural layer with confirmed shape

These were Logan's-call items in the original synthesis. Dispositions are recorded below; commits land *after* the governance audit synthesis (see §9).

### B1. Semantic-lens model carry-forward — disposition: **shape (a), defer with explicit non-decision**

**Original framing:** Two shapes — (a) defer with explicit non-decision in LONG-ARC, or (b) rename `semantic` → `lexical`/`tfidf_lexical` in v0.2.

**Disposition rationale:** Cost asymmetry settles it. Shape (b) is a breaking change for v0.1 consumers paying for a hypothetical post-v0.2 naming-conflict risk; independent xhigh flagged the conflict as quality-not-blocking. Shape (a) preserves optionality: the rename-or-not question can be answered with concrete constraints when a real semantic embedding lens ships (does it replace TF-IDF, sit alongside, or compose with it?). Right now we'd be answering with no concrete constraint other than "the word `semantic` might mislead someone later."

**Edit shape:** Add to `LONG-ARC.md:64-75` Explicit non-decisions list:
> **Semantic-lens embedding-model selection.** v0.1 baseline (TF-IDF + lexical) carries forward into v0.2; the "semantic" lens label is preserved for backward compatibility but does not yet imply dense embeddings. Revisit at v0.3 or earlier if pilot signals warrant. The rename-or-preserve question (whether to retire the `semantic` label in favor of `lexical`/`tfidf_lexical` when a real embedding lens ships) is part of that revisit, not a v0.2 decision.

**Cross-reference:** None needed — `VISION.md:60`'s lens trajectory table already does the disambiguation work and stays as-is.

**What would flip this:** if a v0.2 phase introduces dense embeddings (currently scoped out per `MILESTONE.md:38`), the rename becomes coordinated with the substantive change rather than a standalone breaking change.

### B2. ADR-0005 Option-space steelman expansion — disposition: **shape (a), inline "Considered and rejected" subsection**

**Original framing:** Two shapes — (a) inline ~150-word "Considered and rejected" subsection in ADR-0005, or (b) cite the deliberation document and trim the ADR.

**Disposition rationale:** ADRs are the canonical commit; deliberation documents are session-bound and harder to find for downstream readers six months from now. The independent xhigh's three alternatives (refactor + 0 lenses with paired internal review; abstraction + thin-second-lens scoped to test surface and storage shape; defer abstraction, run two pipelines first) are substantive enough that future architecture readers shouldn't have to follow a deliberation-doc breadcrumb to see why the option space was what it was.

**Edit shape:** After ADR-0005's Decision section (around :38), add ~150-word "Considered and rejected" subsection enumerating (i)/(ii)/(iii) with concrete rejection reasons. One mandatory rejection clause per the audit: for option (ii) (lens abstraction + thin second lens scoped only to test surface and storage shape), reject with "API review against a sketched-but-unimplemented second lens cannot surface storage-shape problems that only manifest when a second lens issues real queries against the substrate — Property 3's coupling is observable only against retrieval-shaped data."

**Constraint on the subsection:** frame as "these alternatives exist; here's why we chose B over them" — not as relitigating closed decisions. The ADR is the commit; the subsection documents the rejected paths.

### B3. Citation storage schema choice criteria — disposition: **shape (b), defer to Phase 14 plan 2 PLAN.md** (this revision flips the original synthesis's lean toward (a))

**Original framing:** Two shapes — (a) specify decision criteria at ROADMAP.md:300 success criterion, or (b) leave success criterion as-is and require Phase 14 plan 2's PLAN.md to specify criteria.

**Disposition rationale (flip):**

- ROADMAP.md:300's existing success criterion already commits the team to "decision recorded with rationale." That's the substantive obligation at the milestone layer.
- Adding criterion-of-criterion (traversal cost, update cost, storage cost, anticipated query patterns) is criterion-creep; the criteria themselves are obvious enough that a competent Phase 14 plan author surfaces them without milestone-layer pre-specification.
- The cross-vendor reviewer is right that the criteria are real. They belong in Phase 14 plan 2's PLAN.md, where they will actually constrain the work, not in ROADMAP where they sit as ratified bullet points.

**Risk this carries:** the Phase 14 plan author forgets to surface the criteria; storage gets decided by what's easy to measure rather than what matters.

**Mitigation:** this synthesis lives in `.planning/audits/` and Phase 14 plan 2 authoring includes reading the synthesis. The synthesis names the finding; the author has the prompt.

**What would flip this:** if the team grew and Phase 14 was authored by someone who wouldn't naturally read the audit synthesis, shape (a) becomes more defensible.

**No edit needed at ROADMAP.md.** The disposition is "leave success criterion as-is; carry the criterion-recommendation forward into Phase 14 plan 2 authoring as a synthesis-derived constraint."

### B4. Phase 17 reporting tripartite — disposition: **ROADMAP edit only; no LPILOT-04 requirement**

**Original framing:** Adopt as ROADMAP edit, or add as new LPILOT-04 requirement code, or both.

**Disposition rationale:** ROADMAP edit only. The tripartite (usage / friction / value-hypotheses) is implementation-of LPILOT-02/03, not its own commitment. New requirement codes are doctrine-level commitments; the tripartite is a reporting-format choice. Conflating them inflates requirement count without adding substance.

**Edit shape:** Replace `ROADMAP.md:357` (Phase 17 success criterion 5) with:
> End-of-window review: pilot artifact produced reporting against three separated categories — **(1) usage evidence** (which lenses were invoked, in what combinations, by user-explicit selection vs default), **(2) friction evidence** (where the multi-lens API was confusing, where lens-disagreement output was unhelpful, where the pilot harness failed to capture intent), **(3) value hypotheses** (what selections persisted — returned-to vs dismissed-once; instances where multi-lens results led to selections single-lens results would have missed, where the data shape supports inference, not as causal claim). The three categories must not be conflated; the artifact reports against schema-version-as-captured.

## 3. Adopted at register layer with narrowed scope

### C1. Provisional vs accepted status for ADR-0005 / MILESTONE.md — disposition: **skip both** (this revision flips the original synthesis's recommendation to add the lighter `audit_synthesis:` frontmatter line)

**Original framing:** Should ADR-0005 be downgraded to "Provisional" pending paired audit synthesis, or kept "Accepted" with a frontmatter `audit_synthesis: ...` pointer?

**Disposition rationale (flip from the original synthesis's lighter-frontmatter recommendation):**

- The substantive remediation is the §9 commit checklist itself. When A1-A7 land, the audit's findings are addressed in the documents themselves; the documents need no separate "I was audited" annotation.
- Future readers wondering whether ADR-0005 was reviewed can find the audit and synthesis in `.planning/audits/2026-04-25-*`. The commit messages of A1-A7 will reference this synthesis explicitly. Git history is the trail.
- Adding `Provisional` retroactively is the kind of register-marker churn that METHODOLOGY discipline D (`METHODOLOGY.md:140-147`) cautions against — it substitutes label-change for substance-change. The lighter `audit_synthesis: ...` frontmatter line does the same thing more cheaply but is still register-marker-not-substance.

**Exception:** if M1 is adopted, METHODOLOGY itself should reference this synthesis as evidence base. That's the only frontmatter pointer this synthesis adds. See §6.

### C2. Rhetorical-label pass on "load-bearing" / "first-class" / "binding" — disposition: **targeted to 1 instance** (narrowed from the original synthesis's 4-6)

**Original framing:** All four audits flag rhetorical-label inflation; the original synthesis suggested 4-6 specific instances.

**Disposition rationale (narrow):** The team's METHODOLOGY discipline D names rhetorical inflation as a pattern to *watch for*, not as a thing to scrub everywhere. Going broader is itself the closure-pressure move the discipline tries to flag — operationalizing "watch for" as "scrub all instances" recapitulates the failure mode at the meta-layer.

**Edits:**

- `VISION.md:34` — rename header from "Load-bearing surface: the multi-lens substrate" to "The multi-lens claim" (or equivalent non-rhetorical label). The argument is in the rest of the section; let the argument do the work.
- `VISION.md:36` — rewrite opening clause from "The architecturally load-bearing claim of this platform is that *no single retrieval lens can serve practicing AI research*" to "The platform's central claim is that no single retrieval lens can serve practicing AI research."

**Skipped from the original synthesis's suggestion:**

- `MILESTONE.md:69` success criterion 1 — already calibrated, no edit.
- `MILESTONE.md:14-20` architectural commitments list — addressed via C3 (status markers), not rhetorical-pass.

"Load-bearing" usage in `LONG-ARC.md` does seam-protection work appropriately and is preserved.

### C3. Milestone success-criteria status markers — disposition: **annotate architectural-commitments list only (MILESTONE.md:14-20); skip success-criteria list (MILESTONE.md:67-75)** (narrowed from the original synthesis's both-lists)

**Original framing:** Should both the architectural-commitments list (`MILESTONE.md:14-20`) and the success-criteria list (`MILESTONE.md:67-75`) be annotated with CLAUDE.md status markers (Settled / Chosen for now / Hypothesis / Open)?

**Disposition rationale (narrow):** The two lists do different work; they don't both need the same treatment.

- The architectural-commitments list (`:14-20`) is a *commitment list* with mixed status: some Settled (`Lens` interface — committed in ADR-0005), some Chosen-for-now (default to semantic for backward-compat — flagged for sunset review per A1), some Hypothesis (citations storage shape — still under spike). Annotating with CLAUDE.md vocabulary is genuine signal.
- The success-criteria list (`:67-75`) is a *test list* — items here are conditions that must be true at milestone completion. Status markers don't apply (Hypothesis success-criterion would be a contradiction in terms; every success-criterion is by definition Settled-as-the-test).

**Edit:** Annotate each item in the architectural-commitments list at `MILESTONE.md:14-20` with `[Settled]`, `[Chosen for now]`, or `[Hypothesis]` per CLAUDE.md vocabulary. First-cut assignments (to be confirmed at edit time):

- `Lens` interface — **Settled**
- `lens=` parameter (default = semantic for backward-compat) — **Chosen for now** (sunset review per A1)
- `ProfileRankingService` becomes lens dispatcher — **Settled**
- Bundle-of-signals profile primitive — **Settled**
- Citations storage shape (normalized vs denormalized) — **Hypothesis** (Phase 14 plan 2)
- `SearchResult` shape generalized — **Settled**
- Lens-disagreement and intersection as MCP-exposed operations — **Settled** (commitment is settled; MCP shape per Phase 16)

## 4. Adopted at doctrine layer (LONG-ARC seam)

### D2. "Not implicit-profile-learning without confirmation" preserved by mechanism — disposition: **adopt now** (this revision promotes from the original synthesis's defer-with-explicit-non-decision tier)

**Original framing:** Logan's call — adopt now or defer to v0.3 doctrine work.

**Disposition rationale (promotion):**

- The seam costs ~50 words. Cheap.
- It protects a real risk: `VISION.md:84`'s "not implicit-profile-learning without confirmation" is anti-vision (negative claim). Anti-vision claims are the easiest to violate by drift; the architecturally-binding form lives in LONG-ARC's protected seams (`LONG-ARC.md:30-40`), not in VISION's anti-vision prose.
- Independent xhigh's #16 caught that the anti-vision is preserved in v0.2 by *deferral* (behavior-derived signals are out of scope per `MILESTONE.md:36`), not by *mechanism*. Adding the seam converts deferral-protection into mechanism-protection at the doctrine layer.
- Deferring to v0.3 is the wrong direction because v0.3 is when behavior-derived signals might enter scope. The seam should be doctrine *before* the implementation question reopens.
- The seam doesn't constrain v0.2 implementation at all (v0.2 has no behavior-derived signals). Pure doctrine that protects the future commitment.

**Edit shape:** Add to `LONG-ARC.md` Protected seams section (around `:30-40`):
> **Explicit profile elicitation.** Any future profile-learning mechanism — behavior-derived (what was read, time-on-page, returned-to, dismissed), citation-anchor-derived, curated-prose, or otherwise — must require explicit researcher confirmation per signal addition. Implicit learning from behavior is foreclosed by VISION's anti-vision (`VISION.md:84`) and re-affirmed here as a v0.2-committed protected seam, even though no v0.2 phase implements it.

## 5. Defer to opportunistic later edit

### D1. Methodology extension scope-creep / discipline F orthogonality (Findings #14, #12, #23)

**Surface:** `.planning/spikes/METHODOLOGY.md`.

The independent xhigh's findings here are real but small. The proposed remediations (split METHODOLOGY into two documents; relabel discipline F as meta-discipline; clarify provenance of disciplines C and D) are all defensible but none is blocking for v0.2 execution. Defer to a future methodology-doctrine commit, or address opportunistically when next editing METHODOLOGY for other reasons.

**Action:** No edit now. Revisit when METHODOLOGY is next edited (e.g., post-governance-audit, if the governance audit synthesizes methodology-doctrine changes).

## 6. Methodology amendment

### M1. METHODOLOGY discipline A — independent dispatch sub-discipline (Hypothesis status)

**Surface:** `.planning/spikes/METHODOLOGY.md:112` (Concrete change clause of discipline A).

**Disposition:** Adopt with `Hypothesis` status marker, before governance audit dispatches. Three reasons:

1. **The contamination cost was measured.** Single comparison; documented effects (finding suppression on 2 net-new categories surfaced in independent xhigh that contaminated missed; one finding flipped from problem to strength under independent reading). Status marker `Hypothesis` per CLAUDE.md vocabulary is the right calibration — single-comparison evidence is suggestive, not strong.
2. **The governance audit cycle is a second test of the discipline.** Applying the forbidden-reading discipline to that dispatch yields either confirmation or weakening. Either outcome is informative; doing the second test under the codified discipline is what `Hypothesis` status calls for.
3. **Not codifying means relying on memory.** Each future paired-audit cycle would re-derive the forbidden-reading instruction. That's the vigilance-over-mechanism failure A7 mitigates against.

**Edit shape (extension to METHODOLOGY.md:112; refined from the original synthesis wording — drops the imprecise "~10-15%" claim):**

> **Concrete change (extension, status: Hypothesis pending second methodology-audit cycle).** Same-vendor adversarial dispatches must not include other audits of the same artifact set as references, even at lower effort levels or earlier dates. Anchoring on prior findings produces delta-on-prior reasoning, which suppresses net-new finding discovery and inherits prior framings even where independent reading would re-evaluate them. The dispatching prompt must include an explicit forbidden-reading list naming all prior audits of the same artifact set; the agent's compliance is verifiable in its tool-use trace. The contaminated 2026-04-25 xhigh dispatch (preserved at `.planning/audits/2026-04-25-v0.2-plan-audit-opus-adversarial-xhigh-contaminated.md`) is the documented evidence base; the comparison at `.planning/audits/2026-04-25-v0.2-plan-audit-comparison.md` characterizes the observed effects. Status remains `Hypothesis` until a second methodology-audit cycle either confirms or weakens the pattern.

**Frontmatter touch on METHODOLOGY.md:** Add `audit_synthesis: 2026-04-25-v0.2-plan-audit-synthesis.md` as a frontmatter field, since M1 is the discipline change derived from this synthesis. This is the *only* frontmatter pointer this synthesis adds (per C1's exception).

## 7. Drop / not adopting

### N1. Discipline B / C overlap (Finding #21, contaminated xhigh only)

The observation that METHODOLOGY disciplines B (model verification) and C (factual-claim verification) substantially overlap is real but small. The independent xhigh did not flag this; instead it surfaced #23 (provenance asymmetry of disciplines C and D), which is sharper. The B/C overlap finding is taste-tier and didn't survive primary-pair scrutiny on its own merits.

**Action:** Drop. If the team revisits METHODOLOGY structure for other reasons, the B/C distinction is one consideration among others.

### N2. "First-class" rhetorical pass everywhere (Finding #8 broader scope)

The independent xhigh's A2 finding flagged a 4-occurrence "first-class" pattern but explicitly downgraded the broad-pass recommendation as taste. Limit to the targeted single-instance pass in C2.

**Action:** Drop the broader rhetorical-pass-everywhere ambition; keep the C2-tier targeted pass.

## 8. Governance-doc audit gap

The paired audit (4 audits across 2026-04-25 dispatches) covered v0.2-specific artifacts only: `MILESTONE.md`, `ROADMAP.md` Phases 12-17, `REQUIREMENTS.md` v0.2 codes, `ADR-0005`, `VISION.md`, `LONG-ARC.md`, `METHODOLOGY.md` practice-disciplines extension.

**Not yet audited:**

- `AGENTS.md`
- ADRs 0001-0004
- `CLAUDE.md` (project root)
- `REQUIREMENTS.md` outside v0.2 codes (the v0.1 / v2-deferred sections)
- `ROADMAP.md` outside Phases 12-17 (Phases 1-11; v2 backlog)
- `STATE.md`
- `.planning/foundation-audit/` notes
- `.planning/ECOSYSTEM-COMMENTARY.md`

**Risk:** the v0.2 plan revisions land with reference to documents that themselves carry closure-pressure / scope-creep / drift the audit didn't see. ADR-0001 in particular is the doctrinal anchor for the multi-lens move; if its register has the same problems as v0.2's, the synthesis edits inherit them. B2 (ADR-0005 option-space steelman) and D2 (LONG-ARC seam) are doctrine-adjacent edits whose shape could change if the governance audit surfaces upstream issues.

**Counter-evidence:** ADR-0001 has been load-tested in the project — it caught the 005-008 drift via the paired methodology audit on 2026-04-25. That drift was *catchable against ADR-0001*, which is evidence ADR-0001's register is doing its job at the doctrine layer.

**Recommendation:** dispatch a governance-doc paired audit (cross-vendor + same-vendor xhigh) scoped to the not-yet-audited set above. Apply M1's forbidden-reading discipline (M1 is committed in Wave 1 specifically so this discipline is operative for the governance dispatch). The cycle costs ~1-2 days of dispatch + synthesis time. The benefit is that B-tier and D2 commits don't get re-revised post-hoc if the governance audit surfaces upstream changes. If the governance audit comes back clean on doctrinal documents, B-tier commits land as-currently-shaped without rework.

**What would skip the governance audit:** confidence that ADRs 0001-0004 and the governance docs are calibrated already (the v0.1 cycle produced them under pressure-tested conditions; ADR-0001 has driven correct architectural behavior). Plus a judgment that the timeline cost of the governance audit exceeds the rework cost of post-hoc B-tier revisions. This synthesis does not recommend skipping; doing the audit is the discipline, skipping because we expect it to be clean is the closure-pressure pattern.

## 9. Sequencing (revised)

The original synthesis §7 proposed an 8-step pre-Phase-12 commit checklist with all commits landing in sequence. This revision splits commits into three waves separated by the governance-doc audit:

### Wave 1 — uncontested edits + methodology amendment

Lands first. Each commit is atomic and revertable. Strictly A-tier (uncontested across primary pair) plus M1 (contamination-prevention discipline that needs to be operative for Wave 2). No B-tier, C-tier, or D2 in this wave.

1. **Plan-revision commit 1 (ADR-0005 reconciliation):** A1 (canonical `lens=` default) + A2 (multi-value `lens=` semantics).
2. **Plan-revision commit 2 (Phase 17 capture):** A3 (multi-attribution capture + change-control rule).
3. **Plan-revision commit 3 (Phase 14 spike pre-registration):** A4 (decision matrix + content-presence verification scope).
4. **Plan-revision commit 4 (milestone-level corrections):** A5 (critical-path) + A6 (backward-compat scope) + A7 (anti-pattern phase-boundary checkpoints).
5. **Methodology commit (M1, marked Hypothesis):** discipline A extension (forbidden-reading) + frontmatter `audit_synthesis:` pointer on METHODOLOGY.md.

After Wave 1, Phase 12 plan 1 authoring can begin; A1/A2 reconciliations are in place.

### Wave 2 — governance-doc audit cycle

6. **Dispatch governance-doc paired audit.** Cross-vendor + same-vendor xhigh; scope per §8. M1's forbidden-reading discipline applies; the forbidden-reading list includes all v0.2-plan-audit artifacts and any prior governance-audit artifacts.
7. **Governance audit synthesis** (separate session). The discipline-A pattern recommends a paired re-read of the synthesis if any of its dispositions become contested.

Wave 2 runs in parallel with Phase 12 plan 1 authoring; it does not block implementation work.

### Wave 3 — post-governance-audit edits

Lands after Wave 2 synthesis is in hand. The B-tier, C-tier, and D2 edits are doctrine-adjacent (they invoke or extend doctrinal claims in ADR-0001 / VISION / LONG-ARC); their final framing benefits from knowing whether the governance audit surfaced issues with the doctrinal substrate they reference.

8. **Plan-revision commit 5 (ADR-0005 + VISION + MILESTONE register):** B2 ("Considered and rejected" subsection) + C2 (VISION.md:34 header + :36 clause) + C3 (architectural-commitments list status markers at MILESTONE.md:14-20).
9. **Plan-revision commit 6 (LONG-ARC doctrine seams):** B1 (semantic-lens explicit non-decision) + D2 (explicit profile elicitation seam).
10. **Plan-revision commit 7 (Phase 17 reporting):** B4 (tripartite reporting at ROADMAP.md:357).
11. **Plan-revision commits derived from governance audit synthesis itself.** Shape and count not predictable in advance.
12. **Optional re-revision of Wave 1 commits** if governance audit surfaces upstream issues affecting them. A-tier edits are tightly scoped and unlikely to need rework, but possible.

**Why these specific groupings in Wave 3:**

- Commit 5 bundles ADR-0005, VISION, and MILESTONE register touches because they are the most likely to need adjustment if governance audit surfaces ADR-0001 or VISION-adjacent issues. Bundling makes the rework-or-land decision atomic.
- Commit 6 bundles the two LONG-ARC seam edits because they touch the same Protected seams / Explicit non-decisions sections.
- Commit 7 (B4) is alone because it touches ROADMAP and is most independent of the doctrinal edits.

### What this revised sequencing buys vs original §7

- B-tier and D2 (doctrine-adjacent) commit risk is bounded: they land *after* the governance audit so upstream doctrinal issues don't propagate.
- A1-A7 + M1 don't wait: they are independent of governance-doc claims and fix concrete document conflicts that block Phase 12 plan authoring.
- M1 is operative for the governance audit dispatch, making the audit a real second test of the contamination Hypothesis.

### What it costs

~1-2 days of governance audit dispatch + synthesis time before the full v0.2 plan-revision cycle closes. Phase 12 plan 1 authoring runs in parallel with the governance audit; it's not blocked.

### What still requires Logan signoff before Wave 1 starts

Confirmation that the dispositions in §§2-4 above match Logan's intent. The dispositions reflect Logan's stated preferences as recorded in conversation 2026-04-26; this synthesis revision is the recorded artifact of those preferences but Logan should review before commits land. A revision summary table:

| Item | Original synthesis | Revised disposition | Change |
|---|---|---|---|
| B1 | shape (a) recommended | shape (a) | None |
| B2 | shape (a) recommended | shape (a) | None |
| B3 | shape (a) leaned | shape (b) | **Flip** |
| B4 | ROADMAP edit only | ROADMAP edit only | None |
| C1 | skip relabel; add lighter frontmatter | skip both | **Flip** |
| C2 | 4-6 instances | 1 instance (`VISION.md:34, :36`) | **Narrow** |
| C3 | annotate both lists | annotate `:14-20` only | **Narrow** |
| D2 | Logan's call (defer or adopt) | adopt now | **Promote** |
| M1 | adopt as Hypothesis | adopt as Hypothesis (refined wording) | Wording |
| Sequencing | 8-step checklist | 3 waves, governance audit between | **Restructure** |

## 10. What this synthesis does not commit

- **Does not modify any audit-target document.** This is a synthesis; the actual edits are downstream commits.
- **Does not adjudicate findings the primary pair did not converge on.** §7 drops two; §5 defers one; the rest are convergent.
- **Does not address the speculative joint blind spots** flagged in the comparison (§4). Those should be spot-checked when Phase 14 / Phase 16 / Phase 12 plan files are drafted; they are not v0.2 plan revisions.
- **Does not pre-empt the governance-doc audit.** The governance audit may surface findings that change Wave 1 commits (especially B2, B1, D2); the revised sequencing accepts that risk explicitly and bounds it via Wave 3.
- **Does not constitute a paired-review cycle on itself.** This synthesis is single-author, and this revision is also single-author. The fact that this revision flips the original on B3 and C1, narrows C2 and C3, and promotes D2 is itself evidence that single-author synthesis is fallible. If any dispositions become contested in implementation, the discipline-A pattern recommends a paired re-read of the synthesis before the affected commit.
