---
status: complete
date: 2026-04-16
target: .planning/spikes/007-training-data-mechanism-probes
---

# Spike 007 Decision

## Decision

### Chosen For Now

1. Treat `SPECTER2` and `Voyage` as the two challengers that should enter `008`.
2. Defer `Stella` and `GTE` rather than promoting them into the first `008` pass.
3. Keep `Qwen3` dropped.

## Why

- `[artifact-reported]` `SPECTER2` and `Voyage` each retain strong target-profile concentration signals plus the strongest prior qualitative support cases from Spike 004.
- `[derived]` `Stella` still has a real open question, but the specific mechanism story remains too proxy-heavy to justify one of the top two slots.
- `[derived]` `GTE` remains coherent and conservative, but does not stay distinct enough to outrank the carried pair under current evidence.

## What This Does Not Decide

1. It does not decide that `SPECTER2` or `Voyage` should become product views.
2. It does not decide that `Stella` or `GTE` are bad models.
3. It does not decide that the blocked citation/community branch no longer matters.

## Resulting Posture For 008

- `[chosen for now]` `008` should compare the incumbent against `SPECTER2` and `Voyage`.
- `[chosen for now]` `Stella` remains the first fallback only if `Voyage` becomes impractical during task execution prep.
- `[chosen for now]` The burden now shifts from explanation to function: the carried challengers must show that their blind-spot papers actually improve research outputs.
