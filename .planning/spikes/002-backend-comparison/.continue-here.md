---
phase: spike-002-backend-comparison
task: DESIGN.md complete, ready for review and execution
total_tasks: 6 dimensions + reference design comparison
status: designing
last_updated: "2026-03-17T12:00:00Z"
branch: spike/001-volume-filtering
---

<current_state>
Spike 002 is designed but not yet executed. The DESIGN.md is thorough and includes infrastructure state, query set, methodological controls, and all 6 experimental dimensions. It needs user review before execution.

Spike 001 A1c (capability envelope) measured SQLite in isolation. Spike 002 measures PostgreSQL at the same scale points and compares. Claims from Spike 001 about "SQLite sufficiency" and "pgvector irrelevance" are treated as hypotheses under test.
</current_state>

<completed_work>

**This session (2026-03-17):**

1. **Git repo fix:** Project extracted from home directory monorepo into standalone `.git` via `git subtree split`. 210 commits preserved. Both `main` and `spike/001-volume-filtering` branches created. GitHub remote set. Home repo's incorrect remote removed.

2. **Spike 001 DESIGN.md updated:** Added capability envelope experiments (A1c), progress table, scope evolution section. DESIGN.md is now the living plan.

3. **A1c.1 TF-IDF benchmark:** 157 MB at 215K papers. Cosine search 516ms (crosses 100ms at ~50-75K). Constraint is compute, not memory.

4. **A1c.2 Concurrent SQLite:** WAL mode eliminates write-induced search degradation completely. Zero lock errors at 100 writes/s.

5. **A1c.3 Embedding benchmark:** Brute-force search 16ms at 215K (30x faster than TF-IDF cosine). GPU 20x faster for embedding computation. CPU cold start 2h (acceptable overnight). Incremental 21s/day.

6. **Deliberation updated:** Three assumptions falsified. Tier model revised. Mitigations documented. BUT: user correctly identified that comparative claims were made with one-sided data.

7. **Spike 002 designed:** 6 dimensions, all measured equally (no pre-judged priorities). Reference design comparison included. Infrastructure state verified. Query set defined.

8. **Epistemic correction:** Spike 001 claims about SQLite sufficiency are hypotheses, not conclusions. Spike 002 provides the comparative data needed for actual conclusions.

**Commits this session (12 total on spike/001-volume-filtering):**
- `fbed800` docs: add project design documents, ADRs, and templates
- `a73de7c` chore: gitignore .claude/ framework tooling
- `71b74ac` docs: add v0.1 phase artifacts
- `fb833c4` docs: add foundation audit and ecosystem commentary
- `ebc4df5` spike(001): expand design with capability envelope experiments (A1c)
- `e52b720` spike(001): complete TF-IDF matrix benchmark (A1c.1)
- `cfd885d` spike(001): complete concurrent SQLite read+write benchmark (A1c.2)
- `5f8b4c5` spike(001): complete embedding benchmark (A1c.3) — capability envelope done
- `ad7be60` docs(deliberation): update deployment-portability with A1c spike findings
- `1c4bfdb` docs(spike-001): restructure roadmap after capability envelope completion
- `b144562` spike(002): design backend comparison experiment

</completed_work>

<remaining_work>

**Immediate next step: Execute Spike 002**

Before running experiments:
1. Review DESIGN.md with user — are dimensions complete? Anything missing?
2. Install pgvector: `sudo apt install postgresql-16-pgvector` (needs user approval)
3. Create spike database: `CREATE DATABASE arxiv_mcp_spike002;`
4. Pre-compute and save 19K paper embeddings as `.npy` file (GPU, ~33 seconds)

Then execute 6 dimensions:
- D1: Search result quality (FTS5 vs tsvector)
- D2: Search latency at scale
- D3: Vector search (pgvector vs brute-force numpy)
- D4: Write performance
- D5: Operational characteristics
- D6: Workflow-level comparison

After experiments:
- Reference design comparison (arxiv-sanity-lite, Semantic Scholar API, etc.)
- FINDINGS.md with side-by-side tables
- Update deliberation with comparative evidence
- DECISION.md only after all data collected

**Spike 001 remaining (independent, lower priority):**
- A2: Corpus visualization (UMAP/BERTopic)
- B1-B3: Signal research
- C1-C3: Tradeoff mapping

</remaining_work>

<decisions_made>

- Spike 001 A1c claims are HYPOTHESES, not conclusions — comparative data required (Spike 002)
- All Spike 002 dimensions measured equally — no pre-judged priorities
- No "fast enough" framing — we report numbers and let users decide
- Reference design comparison provides context beyond our own system
- Deliberation remains OPEN until both-backend data exists
- pgvector needs to be installed (user approval needed for sudo)
- Separate database for Spike 002 experiments (don't pollute project DB)

</decisions_made>

<blockers>

- pgvector not installed (package available, needs `sudo apt install postgresql-16-pgvector`)
- Embeddings from A1c.3 not persisted — need to recompute and save as .npy
- PostgreSQL auth is password-based via TCP (not peer) — connection string: `postgresql://arxiv_mcp:arxiv_mcp_dev@localhost:5432/arxiv_mcp`

</blockers>

<context>

The user (Logan, philosophy PhD) has strong epistemic standards. Key lessons from this session:
- Challenged premature conclusions multiple times (deliberation "concluded" too early, Spike 002 "deprioritized" too early, "fast enough" framing rejected)
- Wants inquiry methodology, not verificationism
- Different users have different demands — we don't declare thresholds on their behalf
- Comparative claims require comparative data
- Reference designs provide context that raw numbers alone cannot

The project is an MCP-native arXiv research discovery tool. v0.1 is complete (13 tools, 4 resources, 3 prompts, 493 tests). The spike program investigates deployment architecture before implementing Phase 11 (distribution) and Phase 12 (storage abstraction).

</context>

<next_action>
Resume from: Spike 002 execution.

The DESIGN.md at `.planning/spikes/002-backend-comparison/DESIGN.md` is the authoritative plan. Read it fully before starting.

First step: confirm pgvector installation with user, create spike database, pre-compute embeddings. Then execute dimensions sequentially (D1 first — search quality).

Files to read on resume:
- `.planning/spikes/002-backend-comparison/DESIGN.md` (spike design — THE PLAN)
- `.planning/spikes/001-volume-filtering-scoring-landscape/FINDINGS.md` (Spike 001 results for comparison baselines)
- `.planning/deliberations/deployment-portability.md` (deliberation context, especially the 2026-03-17 status update)
- `.planning/spikes/ROADMAP.md` (overall spike program status)
- Branch: `spike/001-volume-filtering`
</next_action>
