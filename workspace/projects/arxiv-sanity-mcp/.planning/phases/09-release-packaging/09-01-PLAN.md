---
phase: 09-release-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - CHANGELOG.md
  - pyproject.toml
  - src/arxiv_mcp/cli.py
  - src/arxiv_mcp/mcp/server.py
  - src/arxiv_mcp/ingestion/cli.py
  - tests/test_enrichment/test_service.py
  - tests/test_ingestion/test_arxiv_api.py
  - tests/test_interest/test_ranking.py
  - tests/test_mcp/test_discovery_tools.py
  - tests/test_mcp/test_workflow_tools.py
autonomous: true
requirements:
  - SC-1
  - SC-3
  - SC-4
  - SC-6

must_haves:
  truths:
    - "LICENSE file exists at repo root with verbatim MIT License text including correct copyright year and author"
    - "pyproject.toml has authors, license (SPDX 'MIT'), keywords, classifiers, and [project.urls] section"
    - "CHANGELOG.md exists with v0.1.0 entry in Keep a Changelog format grouped by functional domain"
    - "ruff check src/ tests/ passes with zero errors (96 current errors resolved)"
    - "All 493+ existing tests still pass after ruff fixes"
  artifacts:
    - path: "LICENSE"
      provides: "MIT License text"
      contains: "MIT License"
    - path: "CHANGELOG.md"
      provides: "v0.1.0 release notes"
      contains: "0.1.0"
    - path: "pyproject.toml"
      provides: "Complete PEP 621 metadata"
      contains: "authors"
  key_links:
    - from: "pyproject.toml"
      to: "LICENSE"
      via: "license = 'MIT' references LICENSE file"
      pattern: 'license = "MIT"'
    - from: "pyproject.toml"
      to: "CHANGELOG.md"
      via: "project.urls.Changelog"
      pattern: "Changelog"
---

<objective>
Create LICENSE, complete pyproject.toml metadata, write CHANGELOG.md, and fix all ruff lint errors so CI can enforce linting.

Purpose: These are prerequisites for a distributable release. LICENSE is legally required for open-source. pyproject.toml metadata makes the package discoverable. CHANGELOG documents what v0.1.0 includes. Clean lint is required before CI can gate on ruff.

Output: LICENSE, CHANGELOG.md (new files), pyproject.toml (updated), all source/test files with lint errors fixed, zero ruff errors.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-packaging/09-CONTEXT.md
@.planning/phases/09-release-packaging/09-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LICENSE, update pyproject.toml metadata, write CHANGELOG.md</name>
  <files>LICENSE, pyproject.toml, CHANGELOG.md</files>
  <action>
1. Create LICENSE at repo root with verbatim MIT License text:
   - Copyright line: "Copyright (c) 2026 Logan Rooks"
   - Use exact OSI/SPDX MIT License template (do not paraphrase)

2. Update pyproject.toml — ADD the following fields to the existing [project] table (do NOT remove or modify existing fields like name, version, description, dependencies, scripts):
   ```toml
   authors = [{name = "Logan Rooks"}]
   license = "MIT"
   keywords = ["arxiv", "mcp", "research", "discovery", "papers", "academic", "model-context-protocol"]
   classifiers = [
       "Development Status :: 3 - Alpha",
       "Intended Audience :: Science/Research",
       "License :: OSI Approved :: MIT License",
       "Programming Language :: Python :: 3.13",
       "Topic :: Scientific/Engineering",
       "Topic :: Scientific/Engineering :: Information Analysis",
   ]
   ```
   ADD a new [project.urls] section:
   ```toml
   [project.urls]
   Homepage = "https://github.com/loganrooks/arxiv-sanity-mcp"
   Repository = "https://github.com/loganrooks/arxiv-sanity-mcp"
   Issues = "https://github.com/loganrooks/arxiv-sanity-mcp/issues"
   Changelog = "https://github.com/loganrooks/arxiv-sanity-mcp/blob/main/CHANGELOG.md"
   ```
   Use PEP 639 SPDX identifier format for license (string "MIT", NOT the old {text="..."} or {file="..."} table format).

3. Create CHANGELOG.md in Keep a Changelog 1.1.0 format (https://keepachangelog.com). Single version entry:
   ```
   ## [0.1.0] - 2026-03-14
   ```
   Under "### Added", group features by functional domain:
   - **Ingestion**: OAI-PMH bulk harvesting with resumption tokens, arXiv API client for targeted queries, four time semantics (submission, update, announcement, OAI datestamp), incremental harvesting with checkpoints
   - **Search and Discovery**: Full-text search by title/author/abstract/category/date with AND/OR composition, browse recent announcements by category, find related papers via lexical similarity, cursor-based pagination
   - **Workflow State**: Named collections with add/remove, triage states (unseen/seen/shortlisted/dismissed/read/cite-later/archived) with batch operations, saved queries with on-demand re-run, watches with delta tracking ("what's new")
   - **Interest Modeling**: Interest profiles with seed papers, saved queries, followed authors, and negative examples, signal provenance tracking (manual/suggestion/agent), suggestion engine from workflow activity
   - **Ranking and Explanation**: Composable ranking pipeline with 5 signal scorers, per-result structured ranking explanations, ranker input inspection
   - **Enrichment**: Lazy OpenAlex enrichment (topics, citations, FWCI, related works), bidirectional ID resolution (arXiv/DOI/OpenAlex), provenance tracking per enrichment record
   - **Content Normalization**: Content variants (abstract/HTML/source-derived/PDF-derived markdown), rights-gated content serving per ADR-0003, Marker PDF parsing backend behind extensible adapter interface
   - **MCP Interface**: 13 tools (search_papers, browse_recent, find_related_papers, get_paper, triage_paper, add_to_collection, create_watch, add_signal, batch_add_signals, create_profile, suggest_signals, enrich_paper, get_content_variant), 4 resource templates (paper, collection, profile, watch deltas), 3 prompts (daily-digest, literature-map-from-seeds, triage-shortlist)
   - **CLI**: Full command-line interface mirroring all service capabilities with Rich output and JSON mode

   Include bottom link: `[0.1.0]: https://github.com/loganrooks/arxiv-sanity-mcp/releases/tag/v0.1.0`
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q "MIT License" LICENSE && grep -q "Logan Rooks" LICENSE && python3 -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); assert 'authors' in d['project']; assert d['project']['license'] == 'MIT'; assert 'urls' in d['project']" && grep -q "0.1.0" CHANGELOG.md && echo "PASS"</automated>
  </verify>
  <done>LICENSE exists with MIT text and correct copyright. pyproject.toml has authors, license, keywords, classifiers, and urls. CHANGELOG.md exists with v0.1.0 entry grouped by functional domain.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all ruff lint errors and add per-file-ignores</name>
  <files>pyproject.toml, src/arxiv_mcp/cli.py, src/arxiv_mcp/mcp/server.py, src/arxiv_mcp/ingestion/cli.py, tests/test_enrichment/test_service.py, tests/test_ingestion/test_arxiv_api.py, tests/test_interest/test_ranking.py, tests/test_mcp/test_discovery_tools.py, tests/test_mcp/test_workflow_tools.py</files>
  <action>
1. Add per-file-ignores to pyproject.toml [tool.ruff.lint] section:
   ```toml
   [tool.ruff.lint]
   per-file-ignores = {"alembic/versions/*.py" = ["F401"], "src/arxiv_mcp/cli.py" = ["E402"]}
   ```
   The E402 in cli.py is intentional (Click group registration pattern). Alembic autogenerated files have expected unused imports.

2. Run `ruff check --fix src/ tests/` to auto-fix the 86 fixable errors (78 F401 unused imports, 8 F541 f-string-missing-placeholders).

3. Manually fix the 9 non-auto-fixable errors:
   - F841 (7 unused variables): For each, either prefix with underscore `_` to indicate intentional discard, or remove the assignment if the call has no side effects. Specific files:
     - src/arxiv_mcp/ingestion/cli.py:106 — `task` variable (likely asyncio task, prefix with _)
     - tests/test_enrichment/test_service.py:248 — `result` variable (prefix with _)
     - tests/test_ingestion/test_arxiv_api.py:195 — `params` variable (prefix with _)
     - tests/test_interest/test_ranking.py:230 — `seed_ids` variable (prefix with _)
     - tests/test_mcp/test_discovery_tools.py:44 and 265 — `result` variables (prefix with _)
     - tests/test_mcp/test_workflow_tools.py:62 — `result` variable (prefix with _)
   - F811 (2 redefined-while-unused) in src/arxiv_mcp/mcp/server.py:65-66 — these are variable redefinitions. Read the file to understand context and fix appropriately (likely import side-effect registrations that can be restructured).

4. Run `ruff check src/ tests/` to confirm zero errors.

5. Run `python -m pytest tests/ -x --timeout=30` to confirm no tests were broken by the lint fixes. All 493+ tests must still pass.
  </action>
  <verify>
    <automated>ruff check src/ tests/ && python -m pytest tests/ -x -q --timeout=30</automated>
  </verify>
  <done>ruff check src/ tests/ reports zero errors. All existing tests still pass. per-file-ignores configured for intentional patterns (alembic F401, cli E402).</done>
</task>

</tasks>

<verification>
- `test -f LICENSE && grep -q "MIT License" LICENSE` passes
- `python3 -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); p=d['project']; assert all(k in p for k in ['authors','license','keywords','classifiers'])"` passes
- `test -f CHANGELOG.md && grep -q "0.1.0" CHANGELOG.md` passes
- `ruff check src/ tests/` reports zero errors
- `python -m pytest tests/ --timeout=30` all tests pass
</verification>

<success_criteria>
- LICENSE file at repo root with MIT License text, copyright 2026 Logan Rooks
- pyproject.toml has all PEP 621 metadata fields (authors, license, keywords, classifiers, urls)
- CHANGELOG.md has v0.1.0 entry in Keep a Changelog format with features grouped by domain
- Zero ruff lint errors across src/ and tests/
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-release-packaging/09-01-SUMMARY.md`
</output>
