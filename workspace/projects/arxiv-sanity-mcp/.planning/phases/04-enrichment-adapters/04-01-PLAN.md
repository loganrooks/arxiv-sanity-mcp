---
phase: 04-enrichment-adapters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/arxiv_mcp/db/models.py
  - src/arxiv_mcp/config.py
  - src/arxiv_mcp/enrichment/__init__.py
  - src/arxiv_mcp/enrichment/models.py
  - src/arxiv_mcp/enrichment/openalex.py
  - alembic/versions/004_enrichment_table.py
  - pyproject.toml
  - tests/test_enrichment/__init__.py
  - tests/test_enrichment/conftest.py
  - tests/test_enrichment/fixtures/openalex_work_attention.json
  - tests/test_enrichment/fixtures/openalex_work_not_found.json
  - tests/test_enrichment/fixtures/openalex_batch_response.json
  - tests/test_enrichment/test_models.py
  - tests/test_enrichment/test_adapter.py
autonomous: true
requirements:
  - ENRC-01
  - ENRC-03
  - ENRC-04
must_haves:
  truths:
    - "PaperEnrichment table exists with all required columns for OpenAlex data storage"
    - "OpenAlexAdapter can resolve arXiv IDs to OpenAlex IDs via DOI-based lookup"
    - "OpenAlexAdapter can parse topics, citation counts, FWCI, related works from API response"
    - "Enrichment data includes provenance fields (source_api, api_version, enriched_at)"
    - "Batch DOI resolution works with pipe-separated filter for up to 50 papers"
  artifacts:
    - path: "src/arxiv_mcp/db/models.py"
      provides: "PaperEnrichment ORM model"
      contains: "class PaperEnrichment"
    - path: "src/arxiv_mcp/enrichment/models.py"
      provides: "Pydantic schemas for enrichment data"
      exports: ["EnrichmentResult", "ExternalIds", "TopicInfo", "EnrichmentStatus"]
    - path: "src/arxiv_mcp/enrichment/openalex.py"
      provides: "OpenAlex HTTP adapter with rate limiting"
      exports: ["OpenAlexAdapter", "EnrichmentAdapter"]
    - path: "alembic/versions/004_enrichment_table.py"
      provides: "Database migration for paper_enrichments table"
      contains: "paper_enrichments"
    - path: "src/arxiv_mcp/config.py"
      provides: "Enrichment settings fields"
      contains: "openalex_api_key"
  key_links:
    - from: "src/arxiv_mcp/enrichment/openalex.py"
      to: "https://api.openalex.org"
      via: "httpx AsyncClient with DOI-based resolution"
      pattern: "works/doi:10\\.48550"
    - from: "src/arxiv_mcp/enrichment/openalex.py"
      to: "src/arxiv_mcp/enrichment/models.py"
      via: "response parsing to EnrichmentResult"
      pattern: "EnrichmentResult"
    - from: "src/arxiv_mcp/db/models.py"
      to: "src/arxiv_mcp/db/models.py"
      via: "PaperEnrichment.arxiv_id FK to Paper.arxiv_id"
      pattern: "ForeignKey.*papers\\.arxiv_id"
---

<objective>
Build the enrichment data model, OpenAlex HTTP adapter, and test infrastructure for Phase 4.

Purpose: Establishes the PaperEnrichment table, Pydantic schemas, EnrichmentAdapter protocol, and OpenAlexAdapter HTTP client that all subsequent enrichment work depends on. Creates recorded JSON fixtures and respx-based test infrastructure for mocking OpenAlex API calls.

Output: PaperEnrichment ORM model + migration 004, enrichment Pydantic schemas, OpenAlexAdapter with DOI-based resolution and rate limiting, test fixtures and adapter unit tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enrichment-adapters/04-CONTEXT.md
@.planning/phases/04-enrichment-adapters/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/arxiv_mcp/db/models.py:
```python
class Base(DeclarativeBase):
    pass

class ProcessingTier(enum.IntEnum):
    METADATA_ONLY = 0
    FTS_INDEXED = 1
    ENRICHED = 2
    EMBEDDED = 3
    CONTENT_PARSED = 4

class Paper(Base):
    __tablename__ = "papers"
    arxiv_id: Mapped[str] = mapped_column(String(20), primary_key=True)
    doi: Mapped[str | None] = mapped_column(String(256))
    openalex_id: Mapped[str | None] = mapped_column(String(32))
    processing_tier: Mapped[int] = mapped_column(Integer, default=0)
    promotion_reason: Mapped[str | None] = mapped_column(String(64))
```

From src/arxiv_mcp/config.py:
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore")
    database_url: str = "postgresql+asyncpg://..."
    # ... extend with enrichment settings
```

From src/arxiv_mcp/db/engine.py:
```python
def create_engine(url: str, **kwargs) -> AsyncEngine: ...
def session_factory(engine: AsyncEngine) -> async_sessionmaker[AsyncSession]: ...
```

From tests/conftest.py:
```python
def sample_paper_data(**overrides) -> dict:  # Factory for Paper dicts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrichment data model, Pydantic schemas, and migration</name>
  <files>
    src/arxiv_mcp/db/models.py,
    src/arxiv_mcp/config.py,
    src/arxiv_mcp/enrichment/__init__.py,
    src/arxiv_mcp/enrichment/models.py,
    alembic/versions/004_enrichment_table.py,
    pyproject.toml,
    tests/test_enrichment/__init__.py,
    tests/test_enrichment/conftest.py,
    tests/test_enrichment/fixtures/openalex_work_attention.json,
    tests/test_enrichment/fixtures/openalex_work_not_found.json,
    tests/test_enrichment/fixtures/openalex_batch_response.json,
    tests/test_enrichment/test_models.py
  </files>
  <behavior>
    - Test: EnrichmentResult parses a full OpenAlex Work response with topics, cited_by_count, fwci, related_works, counts_by_year
    - Test: EnrichmentResult handles missing/null FWCI gracefully (fwci=None)
    - Test: EnrichmentResult handles partial response (no topics, no related_works) with status="partial"
    - Test: TopicInfo parses 4-level hierarchy (domain, field, subfield, topic with score)
    - Test: ExternalIds stores short-form OpenAlex ID (W-prefixed, not full URL) and DOI without prefix
    - Test: EnrichmentStatus enum validates allowed statuses (success, not_found, partial, error)
    - Test: PaperEnrichment ORM model can be created and queried with all columns
    - Test: PaperEnrichment unique constraint on arxiv_id prevents duplicate records
  </behavior>
  <action>
    1. Add `respx>=0.22` to `[dependency-groups] dev` in pyproject.toml. Run `uv sync`.

    2. Extend `Settings` in config.py with enrichment fields:
       - `openalex_api_key: str = ""` (free tier API key -- REQUIRED per research: API keys mandatory since Feb 2026)
       - `openalex_api_url: str = "https://api.openalex.org"` (configurable for testing)
       - `enrichment_cooldown_days: int = 7`
       - `enrichment_batch_size: int = 50`
       - `enrichment_rate_limit: float = 5.0` (requests per second)

    3. Create `src/arxiv_mcp/enrichment/__init__.py` with empty package init.

    4. Create `src/arxiv_mcp/enrichment/models.py` with Pydantic v2 schemas:
       - `EnrichmentStatus` as StrEnum: "success", "not_found", "partial", "error"
       - `TopicInfo(BaseModel)`: id (str), display_name (str), score (float), subfield (dict with id + display_name), field (dict), domain (dict)
       - `ExternalIds(BaseModel)`: openalex_id (str|None), doi (str|None) -- short forms, no URL prefixes
       - `EnrichmentResult(BaseModel)`: openalex_id, doi, cited_by_count (int|None), fwci (float|None), topics (list[TopicInfo]|None), related_works (list[str]|None -- full OpenAlex URLs as returned by API), counts_by_year (list[dict]|None), openalex_type (str|None), raw_response (dict), status (EnrichmentStatus), error_detail (str|None), api_version (str -- date string like "2026-03-09")
       - `@classmethod from_openalex_work(cls, work: dict) -> EnrichmentResult`: parses raw OpenAlex Work JSON into structured result. Extracts primary_topic + topics (up to 3). Stores related_works as full URLs. Extracts doi without "https://doi.org/" prefix. Extracts openalex_id as short form (strip "https://openalex.org/"). Sets api_version to current date. Determines status based on field completeness (success if cited_by_count present, partial if missing key fields).

    5. Add `PaperEnrichment` ORM model to `src/arxiv_mcp/db/models.py`:
       - `arxiv_id: Mapped[str]` -- primary_key=True, ForeignKey("papers.arxiv_id", ondelete="CASCADE")
       - `openalex_id: Mapped[str | None]` -- String(64)
       - `doi: Mapped[str | None]` -- String(256)
       - `cited_by_count: Mapped[int | None]` -- Integer
       - `fwci: Mapped[float | None]` -- Float
       - `topics: Mapped[dict | None]` -- JSONB
       - `related_works: Mapped[list | None]` -- JSONB
       - `counts_by_year: Mapped[list | None]` -- JSONB
       - `openalex_type: Mapped[str | None]` -- String(64)
       - `openalex_raw: Mapped[dict | None]` -- JSONB
       - `source_api: Mapped[str]` -- String(32), default="openalex"
       - `api_version: Mapped[str | None]` -- String(32)
       - `enriched_at: Mapped[datetime | None]` -- DateTime(timezone=True)
       - `last_attempted_at: Mapped[datetime]` -- DateTime(timezone=True), nullable=False
       - `status: Mapped[str]` -- String(20), nullable=False
       - `error_detail: Mapped[str | None]` -- Text
       - CheckConstraint: status IN ('success', 'not_found', 'partial', 'error'), name="ck_enrichment_status_valid"
       - Index on status: "idx_enrichments_status"
       - Index on enriched_at: "idx_enrichments_enriched_at"

    6. Create hand-written Alembic migration `004_enrichment_table.py`:
       - revision ID: use alembic revision --autogenerate to get the ID, then hand-write the body
       - down_revision: the 003 migration hash (read from 003_interest_tables.py)
       - upgrade(): create "paper_enrichments" table with all columns, check constraint, indexes
       - downgrade(): drop "paper_enrichments" table

    7. Create test fixtures in `tests/test_enrichment/fixtures/`:
       - `openalex_work_attention.json`: Realistic OpenAlex Work response based on "Attention Is All You Need" structure from 04-RESEARCH.md code examples. Include id, doi, ids, type, is_retracted, cited_by_count, fwci, primary_topic, topics, related_works, counts_by_year, open_access, indexed_in, authorships.
       - `openalex_work_not_found.json`: Empty results response `{"meta": {"count": 0}, "results": []}`
       - `openalex_batch_response.json`: Response with 3 works in results array, each with varying completeness (one full, one without FWCI, one without topics).

    8. Create `tests/test_enrichment/conftest.py`:
       - Import sample_paper_data from root conftest
       - Fixture `openalex_work_fixture()`: loads openalex_work_attention.json
       - Fixture `openalex_not_found_fixture()`: loads openalex_work_not_found.json
       - Fixture `openalex_batch_fixture()`: loads openalex_batch_response.json
       - Fixture `enrichment_session_factory(test_engine)`: creates tables, yields async_sessionmaker, drops tables (same pattern as other test conftest files but yields the factory, not a session)

    9. Create `tests/test_enrichment/test_models.py` with unit tests for all Pydantic schemas.

    RED: Write failing tests first. GREEN: Implement models to pass. Commit separately.
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && uv run pytest tests/test_enrichment/test_models.py -x -q</automated>
  </verify>
  <done>
    PaperEnrichment ORM model exists in db/models.py with all 16 columns, FK to Paper, CHECK constraint, and 2 indexes. Alembic migration 004 creates the paper_enrichments table. Pydantic schemas (EnrichmentResult, ExternalIds, TopicInfo, EnrichmentStatus) validate and parse OpenAlex response structure. Config extended with 5 enrichment settings. respx added to dev deps. Three JSON fixture files exist. All test_models.py tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OpenAlexAdapter with DOI-based resolution, rate limiting, and HTTP tests</name>
  <files>
    src/arxiv_mcp/enrichment/openalex.py,
    tests/test_enrichment/test_adapter.py
  </files>
  <behavior>
    - Test: Single paper enrichment resolves arXiv ID via DOI prefix (doi:10.48550/arXiv.{id}) using singleton endpoint (FREE, 0 credits)
    - Test: Batch enrichment uses filter=doi:{pipe-separated DOIs} endpoint for collection-scoped lookups
    - Test: Batch respects max batch size of 50 (splits larger lists into chunks)
    - Test: Rate limiter enforces minimum interval between requests
    - Test: 429 response triggers exponential backoff retry (up to 3 retries)
    - Test: 404/empty response returns EnrichmentResult with status="not_found"
    - Test: Partial response (missing FWCI or topics) returns status="partial"
    - Test: Adapter uses select= parameter to request only needed fields
    - Test: Adapter includes api_key in request params when configured
    - Test: resolve_ids returns dict mapping arXiv IDs to ExternalIds
    - Test: httpx.AsyncClient is properly closed after use (no resource leaks)
    - Test: Network timeout returns status="error" with error_detail
  </behavior>
  <action>
    Create `src/arxiv_mcp/enrichment/openalex.py`:

    1. Define `EnrichmentAdapter` Protocol class with:
       - `adapter_name: str` property
       - `async def resolve_ids(self, arxiv_ids: list[str]) -> dict[str, ExternalIds]`
       - `async def enrich(self, arxiv_ids: list[str]) -> list[EnrichmentResult]`

    2. Define `SELECTED_FIELDS` constant: comma-separated list of fields to request via select= parameter (id, doi, ids, type, is_retracted, cited_by_count, fwci, citation_normalized_percentile, primary_topic, topics, related_works, counts_by_year, open_access, indexed_in, authorships).

    3. Implement `RateLimiter` class:
       - `__init__(self, requests_per_second: float = 5.0)`: store min_interval, last_request_time
       - `async def acquire(self)`: sleep if needed to respect rate limit using time.monotonic()

    4. Implement `OpenAlexAdapter` class:
       - `__init__(self, settings: Settings)`: store settings, create RateLimiter from enrichment_rate_limit
       - `adapter_name: str = "openalex"`
       - Helper `_build_doi(arxiv_id: str) -> str`: returns `10.48550/arXiv.{arxiv_id}`
       - Helper `_build_params(self) -> dict`: returns base params dict with api_key (if set) and select=SELECTED_FIELDS
       - `async def _request_with_retry(self, client, url, params, max_retries=3, base_delay=1.0) -> httpx.Response`: exponential backoff with jitter on 429. Raises httpx.HTTPStatusError on non-429 errors. Calls rate_limiter.acquire() before each request.
       - `async def resolve_ids(self, arxiv_ids: list[str]) -> dict[str, ExternalIds]`: for single ID use singleton endpoint (FREE). For multiple IDs, batch via filter endpoint. Return dict mapping original arxiv_id to ExternalIds.
       - `async def enrich(self, arxiv_ids: list[str]) -> list[EnrichmentResult]`: for single ID use singleton GET `/works/doi:10.48550/arXiv.{id}`. For multiple, batch via `?filter=doi:{pipe-separated}&per_page=100`. Parse responses with `EnrichmentResult.from_openalex_work()`. Handle not-found (404 for singleton, missing from results for batch) by creating EnrichmentResult with status="not_found". Handle network errors (httpx.TimeoutException, httpx.ConnectError) with status="error". Chunk batch requests to max enrichment_batch_size.
       - Use `async with httpx.AsyncClient(base_url=settings.openalex_api_url, headers={"User-Agent": "arxiv-mcp/0.1.0"}, timeout=30.0) as client:` pattern for proper lifecycle. Create client per enrich/resolve_ids call to avoid stale connection issues.

    5. Create `tests/test_enrichment/test_adapter.py` with respx-mocked unit tests:
       - Use `@respx.mock(base_url="https://api.openalex.org")` decorator pattern
       - Test singleton resolution: mock GET /works/doi:10.48550/arXiv.1706.03762, verify ExternalIds returned
       - Test batch resolution: mock GET /works?filter=doi:..., verify multiple IDs resolved
       - Test batch chunking: provide 60 IDs, verify 2 API calls made (50 + 10)
       - Test single enrich success: mock response with full Work JSON, verify EnrichmentResult fields
       - Test enrich not found: mock 404 response, verify status="not_found"
       - Test enrich partial: mock response missing fwci/topics, verify status="partial"
       - Test 429 retry: mock 429 then 200, verify retry happened and succeeded
       - Test timeout: mock timeout exception, verify status="error" with error_detail
       - Test api_key included in params when set in settings
       - Test select= parameter present in all requests
       - Create a Settings mock with openalex_api_url="https://api.openalex.org" for all tests

    RED: Write failing tests first. GREEN: Implement adapter to pass. Commit separately.
  </action>
  <verify>
    <automated>cd /home/rookslog/workspace/projects/arxiv-sanity-mcp && uv run pytest tests/test_enrichment/test_adapter.py -x -q</automated>
  </verify>
  <done>
    OpenAlexAdapter resolves arXiv IDs to OpenAlex IDs via DOI prefix lookup (singleton for 1 paper, batch filter for multiple). Rate limiter enforces configurable req/s. Exponential backoff handles 429 responses. Not-found and partial responses are correctly classified. All adapter tests pass with respx-mocked HTTP calls (no live API dependency). EnrichmentAdapter protocol defined for future adapter implementations.
  </done>
</task>

</tasks>

<verification>
All enrichment model and adapter tests pass:
```bash
uv run pytest tests/test_enrichment/ -x -q
```

Full test suite still passes (no regressions):
```bash
uv run pytest tests/ -x --timeout=30
```

Migration applies cleanly:
```bash
uv run alembic upgrade head
```
</verification>

<success_criteria>
- PaperEnrichment ORM model in db/models.py with FK to Paper, CHECK constraint, 2 indexes
- Alembic migration 004 creates paper_enrichments table
- EnrichmentResult, ExternalIds, TopicInfo Pydantic models parse OpenAlex Work JSON
- OpenAlexAdapter implements EnrichmentAdapter protocol with DOI-based resolution
- Rate limiter and exponential backoff implemented
- Singleton (FREE) vs batch (10 credits) endpoint strategy correctly implemented
- respx-based test suite with JSON fixtures passes
- Settings extended with 5 enrichment configuration fields
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-enrichment-adapters/04-01-SUMMARY.md`
</output>
