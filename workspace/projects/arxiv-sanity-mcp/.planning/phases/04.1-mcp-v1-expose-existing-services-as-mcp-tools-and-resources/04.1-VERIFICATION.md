---
phase: 04.1-mcp-v1-expose-existing-services-as-mcp-tools-and-resources
verified: 2026-03-12T01:46:43Z
status: passed
score: 6/6 must-haves verified
re_verification: false
---

# Phase 04.1: MCP v1 Verification Report

**Phase Goal:** An MCP server exposes existing search, workflow, interest, and enrichment services as tools and resources. Pre-MCP quality fixes (ranking triple-counting, "seen" triage state) are applied first. An MCP client can perform a complete literature review workflow: search, triage, collect, profile, enrich.
**Verified:** 2026-03-12T01:46:43Z
**Status:** passed
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Category overlap is scored exactly once in the ranking pipeline (effective weight = DEFAULT_WEIGHTS[CATEGORY_OVERLAP], not 2.5x) | VERIFIED | `score_seed_relation` computes author-match only (no `cat_score`). `score_profile_match` computes `0.85*author + 0.15*query_boost` (no category Jaccard). `score_category_overlap` is the sole Jaccard scorer. `test_category_weight_no_triple_count` enforces weight = 0.15. |
| 2 | "Seen" triage state exists and is distinct from absence (never encountered) and from shortlisted/dismissed (decision made) | VERIFIED | `TriageService.VALID_STATES` includes `"seen"`. DB CHECK constraint includes `'seen'`. Migration 007 exists with correct up/down. 6 dedicated triage tests for "seen" all pass. |
| 3 | An MCP client can discover papers via search_papers, browse_recent, find_related_papers, and get_paper tools | VERIFIED | `src/arxiv_mcp/mcp/tools/discovery.py` implements all 4 tools with `@mcp.tool()` decorator. Each delegates to `SearchService` methods. `find_related_papers` supports multi-seed with dedup. 14 discovery tool tests pass. |
| 4 | An MCP client can manage workflow state via triage_paper, add_to_collection, and create_watch tools, plus add_signal for interest profiles and enrich_paper for enrichment | VERIFIED | `workflow.py` implements triage_paper, add_to_collection (with auto-create), create_watch. `interest.py` implements add_signal. `enrichment.py` implements enrich_paper. All use `source="mcp"` for provenance. 11 workflow/interest/enrichment tool tests pass. |
| 5 | MCP resources expose paper, collection, profile, and watch deltas as canonical objects | VERIFIED | `paper.py` registers `paper://{arxiv_id}` (composite view: metadata + triage + enrichment + collections). `collection.py` registers `collection://{slug}`. `profile.py` registers `profile://{slug}`. `watch.py` registers `watch://{slug}/deltas`. `test_resource_count_is_four` enforces exactly 4 resource templates. 9 resource tests pass. |
| 6 | Tool names describe user intent, not implementation | VERIFIED | `test_tool_names_match_expected_set` asserts exact set of 9 user-intent names. `test_no_implementation_leaking_names` asserts no banned terms (embedding, database, sql, etc.). Both tests pass. |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/arxiv_mcp/interest/ranking.py` | Fixed scorers without redundant category Jaccard | VERIFIED | `score_seed_relation` returns `author_score` only (no `cat_score`). `score_profile_match` uses `0.85*author + 0.15*query_boost`. No `cat_score` variable in either function. |
| `src/arxiv_mcp/db/models.py` | CHECK constraint including 'seen' | VERIFIED | Line 261: `"state IN ('seen', 'shortlisted', 'dismissed', 'read', 'cite-later', 'archived')"` |
| `src/arxiv_mcp/workflow/triage.py` | VALID_STATES including 'seen' | VERIFIED | Line 35: `VALID_STATES = {"unseen", "seen", "shortlisted", "dismissed", "read", "cite-later", "archived"}` |
| `alembic/versions/007_add_seen_triage_state.py` | Migration for CHECK constraint update | VERIFIED | 46 lines, upgrade drops old constraint and creates new one with 'seen', downgrade reverses. |
| `src/arxiv_mcp/interest/search_augment.py` | Documented pagination limitation | VERIFIED | `_ranked_search` docstring contains "approximate" and full explanation of over-fetch pagination limitation. |
| `tests/test_interest/test_search_augment.py` | Docstring assertion test | VERIFIED | `test_ranked_search_pagination_documented` asserts `_ranked_search.__doc__` contains "approximate". |
| `src/arxiv_mcp/mcp/server.py` | FastMCP server with lifespan, AppContext | VERIFIED | 84 lines. `AppContext` dataclass with 10 fields (engine, session_factory, settings, 7 services). `app_lifespan` creates all services and disposes engine. `mcp = FastMCP("arxiv-mcp", lifespan=app_lifespan)`. Bottom imports register all tool and resource modules. |
| `src/arxiv_mcp/mcp/tools/discovery.py` | 4 discovery tools | VERIFIED | 159 lines. `search_papers`, `browse_recent`, `find_related_papers`, `get_paper` all decorated with `@mcp.tool()`. |
| `src/arxiv_mcp/mcp/tools/workflow.py` | 3 workflow tools | VERIFIED | 85 lines. `triage_paper`, `add_to_collection`, `create_watch` all decorated with `@mcp.tool()`. |
| `src/arxiv_mcp/mcp/tools/interest.py` | 1 interest tool | VERIFIED | 42 lines. `add_signal` decorated with `@mcp.tool()`. |
| `src/arxiv_mcp/mcp/tools/enrichment.py` | 1 enrichment tool | VERIFIED | 37 lines. `enrich_paper` decorated with `@mcp.tool()`. |
| `src/arxiv_mcp/mcp/resources/paper.py` | Composite paper resource | VERIFIED | 73 lines. Queries Paper ORM, triage state, enrichment status, and collection memberships. Returns composite dict. |
| `src/arxiv_mcp/mcp/resources/collection.py` | Collection resource | VERIFIED | 29 lines. Delegates to `CollectionService.show_collection`. |
| `src/arxiv_mcp/mcp/resources/profile.py` | Profile resource | VERIFIED | 29 lines. Delegates to `ProfileService.get_profile`. |
| `src/arxiv_mcp/mcp/resources/watch.py` | Watch deltas resource | VERIFIED | 29 lines. Delegates to `WatchService.check_watch`. |
| `src/arxiv_mcp/mcp/__main__.py` | Entry point | VERIFIED | 5 lines. Imports `mcp` from server and calls `mcp.run()`. |
| `tests/test_mcp/conftest.py` | Shared test fixtures | VERIFIED | 84 lines. `mock_app_context` and `mock_ctx` fixtures with mock services. |
| `tests/test_mcp/test_discovery_tools.py` | Discovery tool tests | VERIFIED | 252 lines. 14 tests covering all 4 tools. |
| `tests/test_mcp/test_workflow_tools.py` | Workflow/interest/enrichment tests | VERIFIED | 319 lines. 11 tests covering all 5 tools. |
| `tests/test_mcp/test_resources.py` | Resource tests | VERIFIED | 300 lines. 9 tests covering all 4 resources. |
| `tests/test_mcp/test_tool_names.py` | Tool naming tests | VERIFIED | 71 lines. 4 tests: tool count (9), name set, no banned terms, resource count (4). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `mcp/tools/discovery.py` | `search/service.py` | `app.search.search_papers/browse_recent/find_related_papers` | WIRED | Lines 47, 77, 112 delegate to `app.search.*` methods |
| `mcp/tools/workflow.py` | `workflow/triage.py` | `app.triage.mark_triage` | WIRED | Line 34: `await app.triage.mark_triage(...)` |
| `mcp/tools/workflow.py` | `workflow/collections.py` | `app.collections.add_papers/create_collection` | WIRED | Lines 54, 57-58: delegation with auto-create retry |
| `mcp/tools/workflow.py` | `workflow/watches.py` | `app.watches.promote_to_watch` | WIRED | Line 82: `await app.watches.promote_to_watch(...)` |
| `mcp/tools/interest.py` | `interest/profiles.py` | `app.profiles.add_signal` | WIRED | Line 35: `await app.profiles.add_signal(...)` |
| `mcp/tools/enrichment.py` | `enrichment/service.py` | `app.enrichment.enrich_paper` | WIRED | Line 32: `await app.enrichment.enrich_paper(...)` |
| `mcp/resources/paper.py` | Multiple services | `app.triage + app.enrichment + app.collections` | WIRED | Lines 37, 40, 54: composite from 4 services |
| `mcp/server.py` | `db/engine.py` | `create_engine` and `session_factory` in lifespan | WIRED | Lines 48-49: `create_engine(settings.database_url)`, `session_factory(engine)` |
| `mcp/server.py` | `config.py` | `get_settings()` | WIRED | Line 47: `settings = get_settings()` |
| `mcp/server.py` | Tool/resource modules | Side-effect imports at bottom | WIRED | Lines 80, 83: all 4 tool modules + 4 resource modules imported |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| PREMCP-01 | 04.1-01 | Category overlap scored exactly once (remove redundant Jaccard) | SATISFIED | `score_seed_relation` uses author-only. `score_profile_match` uses 85% author + 15% query_boost. `test_category_weight_no_triple_count` enforces 0.15 effective weight. |
| PREMCP-02 | 04.1-01 | "Seen" triage state in CHECK constraint, distinct from absence and decisions | SATISFIED | CHECK constraint updated. `VALID_STATES` includes "seen". Migration 007 exists. 6 tests verify behavior. |
| PREMCP-03 | 04.1-01 | Over-fetch pagination documented as approximate | SATISFIED | `_ranked_search` docstring documents limitation. Assertion test prevents regression. |
| MCP-01 | 04.1-02 | MCP discovery tools: search_papers, browse_recent, find_related_papers, get_paper | SATISFIED | All 4 tools registered on `mcp` instance with `@mcp.tool()`. 14 tests cover param mapping and delegation. |
| MCP-02 | 04.1-03 | MCP workflow/interest/enrichment tools: triage_paper, add_to_collection, create_watch, add_signal, enrich_paper | SATISFIED | All 5 tools registered. All use `source="mcp"`. Auto-create for collections. Two-step watch creation. 11 tests pass. |
| MCP-04 | 04.1-03 | MCP resources: paper://{arxiv_id}, collection://{slug}, profile://{slug}, watch://{slug}/deltas | SATISFIED | All 4 resource templates registered with `@mcp.resource()`. Paper resource provides composite view. 9 tests pass. |
| MCP-06 | 04.1-02 | Tool names describe user intent, not implementation | SATISFIED | `test_tool_names_match_expected_set` validates exact name set. `test_no_implementation_leaking_names` validates no banned terms. |
| MCP-07 | 04.1-02 | MCP tool set stays at 5-10 tools (currently 9) | SATISFIED | `test_tool_count_is_nine` asserts exactly 9 tools. |

**Orphaned requirements:** None. All 8 requirements mapped to Phase 04.1 in REQUIREMENTS.md are claimed by plan frontmatter.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | - | - | - | No anti-patterns found |

No TODO/FIXME/PLACEHOLDER comments, no empty implementations, no stub returns, no console.log-only handlers found in any MCP module.

### Human Verification Required

### 1. MCP Server Startup

**Test:** Run `python -m arxiv_mcp.mcp` and verify the server starts without errors and accepts MCP connections.
**Expected:** Server starts, creates DB engine and all 7 services, and responds to tool/resource listing requests.
**Why human:** Requires a running PostgreSQL database and full service stack initialization. Tests use mocks.

### 2. End-to-End Literature Review Workflow

**Test:** Connect an MCP client (e.g., Claude Desktop) to the server and perform: search_papers -> get_paper -> triage_paper("seen") -> triage_paper("shortlisted") -> add_to_collection -> create_watch -> add_signal -> enrich_paper. Then read paper, collection, profile, and watch resources.
**Expected:** Each tool returns structured results. Resources show updated state (triage_state reflects latest triage, enrichment data appears, collection membership shown).
**Why human:** Integration test requiring real database state, real service coordination, and MCP transport validation.

### 3. Ranking Correctness After Fix

**Test:** Perform a profile-ranked search and inspect the ranking explanation for a paper that matches via categories but has no author match.
**Expected:** The category_overlap signal has weight 0.15 and is the only source of category-based scoring. No hidden category contribution from seed_relation or profile_match.
**Why human:** Requires real paper data with known category overlaps to validate the fix in production context.

### Gaps Summary

No gaps found. All 6 observable truths verified with codebase evidence. All 21 artifacts exist, are substantive (no stubs), and are wired. All 10 key links verified as connected. All 8 requirements satisfied. No anti-patterns detected. 357 tests pass with zero regressions.

---

_Verified: 2026-03-12T01:46:43Z_
_Verifier: Claude (gsd-verifier)_
